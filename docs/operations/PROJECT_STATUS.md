# PROJECT_STATUS.md

Auditoria gerada por leitura completa do código-fonte. Substitui o conteúdo anterior deste arquivo.

Última atualização: 2026-07-04 (Release 1.9 — **Program F Premium Home Experience Wave 1 entregue** — Home redesenhada + nova página `/categorias`, consumindo exclusivamente Market Intelligence/Canonical Catalog/Marketplace Operations/Exchange/Connector Platform, zero regra de negócio em componente React, ISR real (revalidate=60) em vez de force-dynamic; Release 1.8 — **Program D Marketplace Coverage Expansion Wave 1 entregue** — Mega Eletrônicos, Roma Shopping e Atacado Connect certificados com dado real (597 ofertas persistidas, zero fluxo paralelo); **Program C Market Intelligence Engine Wave 1 entregue** — novo domínio `src/domains/market-insights/`: Price Intelligence, Savings Engine, Volatility Rollup, Price History API, Market Pulse canônico — auditoria encontrou sobreposição real com quase todos os objetivos, Merchant Intelligence deliberadamente não duplicado; **Program B Wave 2 (Connector Platform Finalization) entregue** — Delta Import Engine agora operacional em produção (`connector_url_snapshots`, validado com sync real), Connector Registry V2 (`ConnectorDirectoryService`), Observability V2, Source Discovery Policy formalizada; **Program A Wave 5 (Connector Platform V2) entregue** — industrialização da Connector Platform: SDK unificado, Capability Matrix, Certification Framework + Quality Score, Observability, Delta Import (lógica pura), Registry estendido — ver `docs/engineering/CONNECTOR_PLATFORM_V2.md`; Program A Wave 4 (Connector Tier 1 Implementation) permanece **EM ANDAMENTO, pausada pelo CTO** para revisão de arquitetura após recertificar Shopping China (sitemap real, 27.402 URLs de produto confirmadas) e adicionar retry/backoff a `HttpFetchStrategy` — ver `docs/engineering/CONNECTOR_PLATFORM_ARCHITECTURE_REVIEW.md`; migrations das Waves 1-3 aplicadas em produção nesta sessão; **Program C Wave 0 (Merchant Partnership Program) entregue** — nova categoria `docs/business/` (ADR-049), processo oficial de parceria comercial para os 4 merchants que bloqueiam crawlers de IA, zero código, apenas documentação/processo; **Program A Wave 3 (Programa de Certificação de Connectors Tier 1) entregue** — auditoria técnica de 10 merchants candidatos, nova categoria `docs/marketplace/` (ADR-048), zero código de Engine novo por decisão deliberada; **Program A Wave 2 (Real-Time Commerce Engine) entregue** — novo domínio `src/domains/realtime-commerce/`: Change Detection Engine integrado ao `SyncOrchestrator`, Volatility/Freshness/Store Update Intelligence compute-on-read, Market Pulse + Live Activity Feed, Buyer Alert Engine (fundação, sem envio), dashboard interno `/admin/realtime-commerce`)
Branch auditada: `main` — Release 1.7 completo e certificado (ver `docs/operations/RELEASE_CERTIFICATION_1.7.md`); **Release 1.8 (`RELEASE_1_8_BLUEPRINT.md`) com Blueprint aprovado, Sprint Zero, Sprint 0.1, Program 0 Wave 0, Program 0 Wave 1, Program A Wave 1 e Program A Wave 2 entregues** — ADR-043 (fornecedor de câmbio) e ADR-045/046 (Buyer Identity Model completo) resolvidos, ADR-047 (reaproveitamento de `offers.currency`); fornecedor de billing (Wave 8) e fornecedor de notificação (sub-item da Wave 6) seguem pendentes; ponte Brain↔buyer_events (achado do Sprint 0.1) resolvida no Program 0 Wave 0 — mecanismo real, mas volume ainda zero (zero lojas reivindicadas em produção); **Program A Wave 3 (Live Pricing surface na Home) é a próxima entrega planejada do Program A**, já com o Real-Time Commerce Engine pronto para alimentá-la (Market Pulse, Live Activity, Freshness/Volatility badges — ver Epic 11 desta Wave)

> ✅ **Release 1.9 — Program F — Premium Home Experience — Wave 1 (2026-07-04)**: mandato do CTO — a fase de fundação está oficialmente encerrada, foco passa a ser experiência de produto. Home redesenhada + nova página `/categorias`, referência visual obrigatória (imagem enviada), consumindo exclusivamente serviços já consolidados: `MarketplaceMetricsService` (stats reais do Hero, substituindo números fictícios hardcoded), `PriceIntelligenceService.getSavingsOpportunity` (Economia do Dia + Ofertas Relâmpago, Program C — Wave 1), `MarketPulseService`+`VolatilityService` (Market Pulse + Marketplace em Tempo Real, Program A — Wave 2), `ExchangeRateService`/`ExchangeHistoryService` (Câmbio ao Vivo, Program A — Wave 1), `MerchantPriorityService`+`ConnectorDirectoryService` (Lojas em Destaque, quality score + última sincronização reais), `MarketplaceCoverageService` (Categorias, Home top-8 + `/categorias` completa com busca/ordenação/paginação). **Nova camada única de leitura** `lib/home-premium-service.ts` — todo componente de bloco é um Server Component que se auto-alimenta, envolvido em `<Suspense>` individual (streaming granular real, Next.js 16). **Achados reais confirmados ao vivo** (não hipotéticos): `exchange_rates` vazia em produção — `EXCHANGE_RATE_API_KEY` nunca foi configurada neste ambiente, achado honesto, não um bug de código, Câmbio degrada graciosamente; `market_changes` só tem eventos de criação (os 4 conectores do Program D só sincronizaram uma vez cada) — Market Pulse mostra estados vazios honestos em vez de dado fabricado. **Prova real de integração ponta a ponta**: Galaxy S25 Ultra vendido por Shopping China (USD 1099) e Mega Eletrônicos (USD 1080) gera economia real de USD 19 (1,73%) automaticamente no card Economia do Dia — hoje superado pelo iPhone 16 Pro (Cellshop, USD 51/4,86%), confirmando que o mecanismo generaliza. **Performance**: `force-dynamic` trocado por `revalidate=60` (ISR real, confirmado no build: `○ /`, `Revalidate: 1m`); streaming por `<Suspense>` em cada bloco. **Acessibilidade**: `aria-label` adicionado às buscas (Home + `/categorias`), hierarquia de headings validada (h1 único por página), globo decorativo do Hero marcado `aria-hidden`. **Auditoria prévia** eliminou `Stats.tsx` (duplicava os números que o Hero já mostra) e reaproveitou `AIShowcase`/`HowItWorks`/`Brands`/`ForLojistasSection`/`CTASection` sem alteração. 3 extensões aditivas mínimas: `ICanonicalCatalogRepository.findAll()`, `StoreCard` ganhou `qualityScore`/`lastSyncAt` opcionais (zero breaking change nos outros 3 consumidores). Verificado com dev server real rodando (não apenas build) — todas as páginas/blocos confirmados renderizando com dado real, um link morto real encontrado e corrigido (produto vencedor de uma economia usa slug do produto bruto, não do produto canônico). Quality Gate: lint 0, typecheck 0, build OK, 524/524 testes. **Limitações honestas**: Câmbio depende de credencial externa não provisionada; Câmeras ao Vivo é arquitetura sem integração (conforme mandato); chat da IA não implementado (conforme mandato, "apenas preparar integração").
>
> ✅ **Release 1.8 — Program D — Marketplace Coverage Expansion — Wave 1 (2026-07-04)**: mandato do CTO — "a fase de fundação está encerrada"; implementar Mega Eletrônicos/Roma Shopping/Atacado Connect usando exclusivamente a infraestrutura já consolidada, zero evolução arquitetural salvo correções pontuais inevitáveis. **Auditoria ao vivo** (não apenas robots.txt/sitemap como a Wave 3, mas fetch real de sitemap + página de produto de cada loja) confirmou/corrigiu a auditoria original: Atacado Connect tem sitemap agora completo (~18.000 produtos, não incompleto como reportado em 2026-07-03) e usa JSON-LD schema.org por produto (o sinal mais limpo dos 3); Roma Shopping usa Open Graph (`product:price:amount`/`product:availability`); Mega Eletrônicos exibe preço em formato numérico americano (vírgula-milhar, ponto-decimal), o oposto do padrão latino já assumido pelo SDK — corrigido com uma função nova e isolada (`parseAmountUSFormat`), Shopping China inalterado. **3 Connectors implementados**, cada um 100% dentro do pipeline existente (Validation→Normalization→Deduplication→ProductIdentityShadow→Media→CatalogWrite→MarketChangeDetection), reutilizando Delta Import/Capability Matrix/Certification/Observability sem nenhum código novo de plataforma. **Correções pontuais inevitáveis**: `parseAmountUSFormat` (SDK), `CanonicalOfferView.productId` + `ICanonicalCatalogRepository.findByCategoryId`/`findCanonicalProductIdByProductId` (já existiam do Program C Wave 1, reutilizados), 2 correções de dado (`stores` row nova para Roma Shopping, rename de "Atacado Games"→"Atacado Connect" refletindo rebranding real do site). **Validado com execução real em produção** (autorizado pelo CTO em 2 etapas — 5 produtos, depois 200): 197+205+206 = 597 ofertas reais persistidas, 0 falhas na versão final (Roma Shopping teve 1 falha transitória na 1ª tentativa por `stores` ausente, corrigida antes do sync completo). **Canonical Match real comprovado**: um "Galaxy S25 Ultra 256GB" agora vendido por Shopping China (USD 1099) E Mega Eletrônicos (USD 1080, novo) — `PriceIntelligenceService` (Program C Wave 1) calculou automaticamente economia real de USD 19 (1.73%) sem nenhum código novo, prova concreta de que Market Intelligence generaliza para novos merchants. Os 3 conectores aparecem corretamente em `ConnectorDirectoryService`/`ConnectorObservabilityService` com health scores 100/80/100 e quality scores 69/65/72, também sem código novo. **Canonical Match rate honesto e baixo** (1.0%/0.0%/0.5%) — Product Identity em Shadow Mode, catálogos de eletrônicos/perfumes com variantes muito específicas. **Achado de tooling** (não bloqueante): Jest não consegue rodar testes unitários de parsers que importam `node-html-parser` (dependência transitiva ESM-only) — pré-existente, não introduzido por esta Wave; parsers afetados validados contra HTML real ao vivo em vez de testes unitários. Quality Gate: lint 0, typecheck 0, build OK, 524/524 testes, `db:lint` OK (14 migrations, inalterado — zero tabela nova). Mobile Zone/Visão VIP seguem bloqueados até ADR de headless browser.
>
> ✅ **Release 1.8 — Program C — Market Intelligence Engine — Wave 1 (2026-07-04)**: mandato do CTO — transformar preços em inteligência reutilizável, sem dashboards/páginas/IA generativa. **Auditoria prévia obrigatória encontrou sobreposição real e significativa** com quase todos os 10 objetivos: `CanonicalPriceHistoryService` (canonical-catalog) já tinha min/max/avg/trend histórico; `VolatilityEngine` (realtime-commerce Wave 2) já é o motor de volatilidade estatística; `ExchangeAnalyticsService.computeBuyerSavings` (exchange Wave 1) já calcula economia, embora catalog-wide, não per-produto; e Merchant Intelligence (Objetivo 7 do brief) já está quase inteiramente coberto por `StoreUpdateIntelligenceService`+`ConnectorObservabilityService`+`MarketplaceCoverageService`+`ProductHealthService` juntos. **Resolução**: novo domínio de fundação `src/domains/market-insights/` (como `canonical-catalog`/`exchange`/`realtime-commerce`, depende deles livremente, nenhum depende de volta) contendo apenas o que era genuinamente novo — `PriceIntelligenceService` (mediana + dispersão entre lojas via coeficiente de variação, que `CanonicalPriceHistoryService` não calculava), `computeSavingsOpportunity` (economia per-produto-canônico "Loja X vs Loja Y", distinto do savings catalog-wide de Exchange), `VolatilityRollupService` (rollup do `VolatilityEngine` já existente para nível canônico/categoria/merchant — nenhuma estatística nova), `PriceHistoryQueryService` (API interna, nunca pública, compõe `CanonicalPriceHistoryService`+`market_changes`), `MarketPulseInsightsService` (rollup canônico de `MarketPulseService.getTopMovers`, que já responde "lojas mais atualizaram"/"categorias mais movimentadas" sem qualquer wrapper novo). **Objetivo 7 (Merchant Intelligence) deliberadamente não implementado** — documentado com mapeamento explícito de cada métrica pedida ao serviço já existente que a cobre. 3 extensões pequenas e aditivas em `canonical-catalog` (auditadas, zero breaking change): `CanonicalOfferView.productId`, `ICanonicalCatalogRepository.findByCategoryId`/`findCanonicalProductIdByProductId`, `CanonicalPriceAggregation.lastPriceUSD`/`firstSeenAt` (valores já computados internamente, só não expostos). **Zero tabela nova** — tudo compute-on-read. Zero `BrainAsset` novo — mapeamento para os 6 já existentes documentado, criação fica para decisão própria futura. 23 testes novos (funções puras priorizadas). Docs: `docs/engineering/MARKET_INTELLIGENCE_ENGINE.md` (novo, relatório completo da auditoria). Quality Gate: lint 0, typecheck 0, build OK, 506/506 testes, `db:lint` OK (14 migrations, inalterado). **Limitação honesta central**: o valor real desta camada é proporcional à cobertura de Canonical Match, hoje baixa (só Shopping China com Connector certificado) — estatisticamente correto, mas pouco significativo até a expansão de Connectors ser retomada.
>
> ✅ **Release 1.8 — Program B — Wave 2 — Connector Platform Finalization (2026-07-04)**: mandato do CTO — resolver exatamente as 2 limitações honestas nomeadas ao final da Wave 5 (Registry com bootstrap linear; Delta Import sem persistência), sem implementar novos merchants. **Delta Import Persistence**: migration `20260704090000_connector_url_snapshots.sql` (auditada contra `DATABASE_ENGINEERING.md` antes de escrever — confirmado que não duplica `market_changes`, que registra mudança já comparada, não o sinal bruto do `<lastmod>` do sitemap) — tabela única, upsert por `(connector_id, url)`, RLS sem policy pública. `IConnectorUrlSnapshotRepository`/`SupabaseConnectorUrlSnapshotRepository` (dentro de `connectors/`, sem risco de dependência circular). **Achado real corrigido durante a implementação**: `IConnector.fetch()` não tinha como saber se a sincronização era dry-run — gravar snapshot incondicionalmente teria violado a invariante "dry-run nunca escreve"; corrigido com `ConnectorFetchOptions` opcional (aditivo, nenhum connector existente precisou mudar). **Validado com execução real, não apenas dry-run**: sync real (3 produtos) gravou dados reais + 6 eventos em `market_changes`; sync seguinte (dry-run) confirmou exatamente 3 URLs puladas de 27.418 — Delta Import genuinamente operacional em produção. **Connector Registry V2**: `ConnectorRegistryImpl` não mudou de forma (continua índice em memória puro); `ConnectorDirectoryService` (`lib/`) nova, responde merchant/versão/capabilities/status/certificação/health/quality score em duas granularidades (`listAll()` barato, `getDetail()` caro sob demanda) — zero reflexão, zero plugin loader, zero DI framework. `bootstrap.ts` permanece registro estático por import — restrição real do bundler da Vercel, não dívida adiada. **Observability V2**: `productsProcessedLastSync`/`failuresLastSync` (do sync mais recente), `productsChanged`/`offersChanged` (janela de 24h sobre `market_changes`), `qualityScore` — tudo reaproveitando dado já existente, nenhuma tabela nova. **Source Discovery Policy**: formalizada em `docs/engineering/SOURCE_DISCOVERY_POLICY.md` — hierarquia obrigatória (API oficial → parceiro → feed → sitemap → structured data → HTML) e proibições absolutas (nunca Cloudflare/robots.txt/anti-detecção, headless browser exige ADR). **Governança**: auditoria de direção de dependência confirmou zero violações (`connectors/` segue sem depender de `marketplace-operations`/`catalog-intelligence`/`exchange`); 1 inconsistência de nomenclatura corrigida (`RateLimiter.ts` → `RateLimitedFetchStrategy.ts`, alinhando com a convenção já existente); 1 inconsistência pré-existente (`ConnectorRegistryImpl`) auditada e deliberadamente não tocada (baixo valor, alto custo de churn). Docs: `docs/engineering/CONNECTOR_PLATFORM_V2.md` §14 (novo), `SOURCE_DISCOVERY_POLICY.md` (novo), `TECH_DEBT.md` atualizado. Quality Gate: lint 0, typecheck 0, build OK, 480/480 testes, `db:lint` OK (14 migrations). **Retomada dos 5 merchants pendentes (Wave 4) segue dependendo de decisão explícita do CTO — a Connector Platform está pronta tecnicamente.**
>
> ✅ **Release 1.8 — Program A — Wave 5 — Connector Platform V2 (2026-07-04)**: mandato do CTO — Architecture Review completa da Connector Platform seguida de industrialização, **sem implementar novos merchants** (Mega Eletrônicos/Roma Shopping/Atacado Connect/Mobile Zone/Visão VIP permanecem congelados). Auditoria contra 13 objetivos de arquitetura (Runtime, Registry, SDK, Capability Matrix, Certification, Quality Score, Delta Import, Snapshot Engine, Observability, Event-Driven, Source Discovery, Reuso, Preparação para escala) — cada um resolvido em um de três vereditos: já existe (reaproveitado), construído nesta Wave, ou deliberadamente não construído (razão documentada). **Connector SDK** (`src/domains/connectors/sdk/`) unifica a superfície de import — módulos novos desta Release movidos fisicamente (`HttpFetchStrategy`+retry, novo `RateLimitedFetchStrategy`, `SitemapCrawler`+`DeltaImportPlanner`, `textParsing`), módulos pré-existentes e já testados (`SitemapParser`/`RobotsParser`/field mappers) apenas re-exportados no lugar para não gerar churn cosmético. **Capability Matrix** (`ConnectorCapabilities`, campo obrigatório em `ConnectorMetadata`) — declaração honesta expôs que Shopping China não tem `supportsStock` real (estoque é `true` fixo, sem indicador na página). **Certification Framework + Connector Quality Score** (`lib/connector-certification-service.ts`) — o agregador cuja ausência era dívida técnica nomeada desde a Wave 3, construído agora que as tabelas de Real-Time Commerce/Exchange/Marketplace Operations estão em produção (13 critérios, `Analytics`/`Brain` reportados como "não avaliado" nunca "falhou" para lojas sem merchant vinculado). **Connector Observability** (`lib/connector-observability-service.ts`) compõe Health+Freshness+Volatility sem recomputar nenhum. **Delta Import Engine**: lógica pura construída e testada (`DeltaImportPlanner`), mas **não conectada a nenhum Connector real** — persistir `lastmod` entre execuções exigiria uma tabela nova sem migration autorizada, e um cache em memória teria efeito zero em produção serverless; documentado, não fingido. **Snapshot Engine**: já existe como `market_changes` (Wave 2) — não duplicado. **Event-Driven**: auditado e deliberadamente não reescrito (2 consumidores estáveis não justificam um barramento de eventos genérico hoje). **Source Discovery**: prioridade já existe como política real (`Integration Strategy`, Wave 3), formalizada em documento. 28 novos testes (SitemapCrawler/textParsing/DeltaImportPlanner/RateLimitedFetchStrategy — o gap nomeado na Wave 4). Docs: `docs/engineering/CONNECTOR_PLATFORM_V2.md` (novo, relatório completo), `TECH_DEBT.md` atualizado. Quality Gate: lint 0, typecheck 0, build OK, 480/480 testes, `db:lint` OK — validado com dry-run real ao vivo contra `shoppingchina.com.py` após o refactor do SDK. **Retomada dos 5 merchants pendentes segue dependendo de decisão explícita do CTO.**
>
> ⏸️ **Release 1.8 — Program A — Wave 4 — Connector Tier 1 Implementation (2026-07-04, EM ANDAMENTO)**: mandato do CTO — implementar os primeiros Connectors certificados para os 4 merchants tecnicamente viáveis identificados na Wave 3 (Shopping China, Mega Eletrônicos, Roma Shopping, Atacado Connect), mais um spike de confirmação para Mobile Zone/Visão VIP. **Antes de qualquer execução real, 2 bloqueios foram encontrados e resolvidos com o CTO**: (1) as migrations das Waves 1-3 (`marketplace_operations`, `exchange_intelligence`, `realtime_commerce`) nunca haviam sido aplicadas em produção — sem elas, nenhum Connector poderia provar Change Detection/Exchange/Marketplace Operations, critérios exigidos pela certificação; aplicadas nesta sessão via `npm run db:push`, autorizado explicitamente pelo CTO; (2) Mobile Zone e Visão VIP estavam marcados "Needs Technical Spike" (não "Ready for Connector Build") — confirmado, via inspeção de estrutura de página (payload RSC vazio / SPA React pura), que ambos são genuinamente client-side-rendered; nenhuma dependência de headless browser (Playwright/Puppeteer) existe no projeto, então nenhum código foi escrito para essas 2 lojas. **Shopping China recertificado**: descoberta de produto migrada de 3 categorias hardcoded para o sitemap real (`shoppingchina.com.py/sitemap.xml`, confirmado ao vivo — 27.402 URLs de produto), validado com dry-run real contra o site (5 produtos reais buscados/parseados/validados/normalizados/deduplicados com sucesso pelo pipeline completo). **`HttpFetchStrategy` ganhou retry/backoff** (mesma política linear de `ExchangeRateApiHttpClient`), fechando o gap nomeado na Wave 3 — herdado automaticamente por todo Connector sitemap-driven futuro. Dois módulos novos compartilhados (`crawler/shared/`): `SitemapCrawler` (anda sitemap/sitemap-index, reaproveita `SitemapParser` do Discovery) e `textParsing.ts` (parsing de preço/moeda no padrão regional). **O CTO pausou a implementação dos 3 novos Connectors (Mega Eletrônicos, Roma Shopping, Atacado Connect) para exigir uma revisão de arquitetura completa antes de prosseguir** — entregue como `docs/engineering/CONNECTOR_PLATFORM_ARCHITECTURE_REVIEW.md` (arquitetura em camadas, pipeline de 7 estágios, estrutura de diretórios completa, componentes reutilizáveis, integrações cross-domain reais, cobertura de testes, achados relevantes para os próximos Connectors — nenhuma `stores` row confirmada para as 3 lojas do próximo lote, decisão de moeda canônica não é automática). Quality Gate: lint 0, typecheck 0, build OK, 452/452 testes, `db:lint` OK — nenhum código novo para os 3 Connectors pendentes, nenhum teste novo ainda para `SitemapCrawler`/`textParsing.ts` (nomeado em `TECH_DEBT.md`). **Retomada requer decisão explícita do CTO após revisar o documento de arquitetura.**
>
> ✅ **Release 1.8 — Program C — Wave 0 — Merchant Partnership Program (2026-07-03)**: mandato do CTO — construir o programa oficial de parcerias comerciais para os merchants Tier 1 que a auditoria da Wave anterior (Program A Wave 3) identificou como bloqueados para scraping (Cellshop, Nissei, Casa Americana, New Zone bloqueiam `ClaudeBot` nomeadamente no `robots.txt`). Decisão arquitetural reafirmada: nunca contornar `robots.txt`, nunca burlar Cloudflare, nunca criar scraping que viole política pública — o caminho para o dado dessas lojas passa a ser exclusivamente comercial. Nova categoria **`docs/business/`** (ADR-049, 10ª categoria do Knowledge System — `docs/README.md`/`FOUNDATION_INDEX.md`/`CLAUDE.md` atualizados), distinta de `docs/marketplace/` (que documenta fatos técnicos sobre as lojas, não o processo comercial do ParaguAI com elas). 4 documentos entregues: `MERCHANT_PARTNERSHIP_PROGRAM.md` (visão, 4 tiers de parceria — Integration Partner/Certified Merchant/Official Merchant Partner/Founding Partner, explicitamente distintos de `OwnershipLevel.PremiumMerchant` do auto-serviço de claim, Release 1.7 Wave 5 —, fluxo de 9 estágios de Contato inicial a Partner Oficial, Partner Integration Requirements mapeados 1:1 a `RawOffer`/`RawProduct` já existentes, Merchant Integration Score reaproveitando `ConnectorHealthService`/`FreshnessService`/`ProductHealthService`/`MarketplaceCoverageService` — zero Engine novo), `TIER1_PARTNERS.md` (pipeline de negociação dos 10 merchants Tier 1, ordem de contato recomendada priorizando os 4 bloqueados), `PARTNERSHIP_PROPOSAL.md` e `PARTNERSHIP_EMAIL_TEMPLATE.md` (ambos em PT/ES/EN, tom profissional, nenhuma menção a scraping/bots/Cloudflare). Formatos de integração reaproveitam `ConnectorType` já existente (`JsonFile`/`CsvFile`/`ApiRest`/`XmlFile`/`ManualUpload`/`Erp` já cobrem 6 dos formatos pedidos; GraphQL/FTP/SFTP/Webhook/Google Sheets ficam como extensão aditiva futura, nomeada em `TECH_DEBT.md`, sem implementação nesta Wave). Achado-chave: uma vez que um feed de parceiro seja implementado como `IConnector`, ele entra no mesmo pipeline (`ValidationStage → ... → MarketChangeDetectionStage`) que qualquer Connector de scraping — todo o Real-Time Commerce Engine, Catalog Intelligence e Connector Health já se aplicam automaticamente, sem nenhuma modificação de arquitetura. **Zero código de produto escrito nesta Wave** — mandato explícito do CTO ("nenhum código de scraping deverá ser criado"), Wave puramente de documentação/processo. Nenhum contato real foi feito com nenhum merchant ainda. Docs: `ROADMAP_1_8.md` (Program C ganha Wave 0, antes da Wave 4 já planejada — nota de nomenclatura documentando a coincidência de letra com "Marketplace Intelligence"), `TECH_DEBT.md` (5 formatos de integração ausentes, campo de latência não computável, SKU/EAN/GTIN não são campos de primeira classe). Quality Gate: nenhum código de produto alterado — lint/typecheck/build/testes inalterados em relação à Wave 3.
>
> ✅ **Release 1.8 — Program A — Wave 3 — Programa de Certificação de Connectors Tier 1 (2026-07-03)**: mandato do CTO — auditar 10 lojas Tier 1 (Shopping China, Cellshop, Nissei, Mega Eletrônicos, Casa Americana, Roma Shopping, Mobile Zone, New Zone, Atacado Connect, Visão VIP) e criar um processo oficial de certificação de Connector, reaproveitando exclusivamente infraestrutura já existente — **zero Engine novo, zero domínio novo, conforme exigido explicitamente**. Antes de qualquer escrita, uma auditoria interna (paralela à externa) encontrou 4 conflitos reais com o mandato do CTO ("interrompa e apresente antes de implementar"), todos resolvidos com o CTO antes de prosseguir: (1) a "Marketplace Priority" pedida (Tier/Business Priority/Marketplace Value/Catalog Size/Sync Frequency) duplicava 5 dos 7 fatores já computados por `MerchantPriorityService` (Program 0 Wave 1) — resolvido reaproveitando-o para lojas já onboarded e usando a fórmula de pré-onboarding já existente em `RELEASE_1_8_BLUEPRINT.md` Capítulo 1 ("Prospect Score") só para prospects, nunca um segundo sistema de tiers; (2) "Health" no Connector Score = `ConnectorHealthService` já existente; (3) "Canonical Match Rate" = lógica já inline em `MerchantPriorityService`; (4) `docs/marketplace/` (caminho pedido no brief) não era uma das 8 categorias oficiais do Knowledge System — resolvido com **ADR-048**, formalizando-a como 9ª categoria (`docs/README.md`/`FOUNDATION_INDEX.md`/`CLAUDE.md` atualizados no mesmo commit). **Achado de conformidade crítico, não previsto no brief**: auditoria técnica real (robots.txt + fetch de páginas públicas) encontrou que **4 das 10 lojas — Cellshop, Nissei, Casa Americana e New Zone — bloqueiam explicitamente crawlers de IA nomeando `ClaudeBot`** em seu `robots.txt` (Cellshop/Nissei também com Cloudflare 403 ativo independente de user-agent). Nenhum scraping foi tentado ou deve ser tentado contra elas. Confirmado com o CTO: essas 4 permanecem Tier 1 e prioritárias no roadmap, documentadas com `Certification Status: Restricted — Commercial Partnership Recommended` e novo campo `Integration Strategy: Data Partnership` (novo campo criado em `docs/marketplace/Tier1_Merchants.md`, valores possíveis: Public Connector/Commercial API/Data Partnership/Merchant Feed/Manual Import/Pending Decision) — arquitetura já preparada para receber feed/API oficial futuramente sem refatoração (o pipeline de conectores é agnóstico à origem do dado: HTML scraping, feed JSON/CSV ou API, todos produzem o mesmo `RawOffer[]`). Das 6 lojas restantes: Shopping China (connector já existe, mas não certificado — não usa o sitemap real, 3 categorias hardcoded), Mega Eletrônicos, Roma Shopping (~50k produtos estimados, maior catálogo) e Atacado Connect (ex-"Atacado Games" do Blueprint) ficam **Ready for Connector Build** (sitemap declarado, sem anti-bot, HTML estático); Mobile Zone e Visão VIP ficam **Needs Technical Spike** (indícios de renderização client-side não confirmados por ferramenta de navegador real). Processo de certificação: 15 critérios (`docs/marketplace/Tier1_Merchants.md` §2), todos mapeados a serviços já existentes (Connector Platform, `MarketplaceCoverageService`, `ProductHealthService`, `MerchantPriorityService`, `ConnectorHealthService`, `FreshnessService`/`StoreUpdateIntelligenceService`, `MarketplaceAlertService`, `AutomaticCurrencyService`) — limiares numéricos deliberadamente não fixados (sem dado de produção real de nenhum Connector Tier 1 novo ainda). Nenhum código de agregador de score foi escrito (decisão do CTO: "o sucesso não será medido pela quantidade de código") — fica nomeado em `TECH_DEBT.md` como próximo passo técnico. Docs: novo `docs/marketplace/` (README + `Tier1_Merchants.md`, ~10 perfis completos), ADR-048 em `DECISIONS.md`, 6 gaps novos em `TECH_DEBT.md`. Quality Gate: sem código de produto alterado — apenas documentação e um ADR; nenhum lint/typecheck/build/teste novo aplicável.
>
> ✅ **Release 1.8 — Program A — Wave 2 — Real-Time Commerce Engine (2026-07-03)**: mandato do CTO — construir o motor que detecta, interpreta, registra e distribui toda mudança de mercado da fronteira em tempo quase real (12 Epics no brief original), não apenas atualizar preços. Domínio novo `src/domains/realtime-commerce/` (types/enums/change-detection/volatility/freshness/services/market-pulse/alerts/dashboard/repositories/infrastructure/events/`__tests__`) — Core Asset que todo o Marketplace pode depender, nunca o contrário (Epic 1). `ChangeDetector` (Epic 2, puro, sem I/O) diffa um snapshot antes/depois de uma offer em até 9 tipos de mudança (preço subiu/desceu, estoque voltou/saiu/quantidade mudou, produto/offer criados, imagem/descrição/categoria/marca mudaram) — uma queda de preço ≥15% (constante nomeada, `PROMOTION_PRICE_DROP_THRESHOLD`) também dispara `PromotionDetected`. **Integrado de verdade ao pipeline de conectores**, não isolado: `DeduplicationStage` agora carrega o `existingSnapshot` pré-escrita (campo novo em `DeduplicatedOffer`), `CatalogWriteStage` passa a devolver `storeId` em `PersistenceResult`, e um novo estágio `MarketChangeDetectionStage` roda após a persistência chamando `ChangeDetectionService.detectAndRecord` — mesmo padrão de estágio que `ProductIdentityShadowStage` já usa para depender do Core Asset de Product Identity sem o dono. `SyncOrchestrator` ganhou o construtor parâmetro `changeDetectionService` (assinatura e os 4 testes existentes atualizados, suíte de `connectors` 59/59 intacta). Migration `20260703170000_realtime_commerce.sql`: `market_changes` (ledger append-only, fonte única de verdade — Volatility/Freshness/Store Update/Market Pulse são todos derivados por leitura, nunca duplicam estado), `market_pulse_snapshots` (rollup diário upsert-by-date, mesmo padrão de `marketplace_health_snapshots`), `buyer_alert_candidates` (índice único em `rate_limit_key` — o rate limiting é uma constraint de banco, não um cache em processo). `VolatilityEngine` (Epic 3, compute-on-read, 4 fatores explicáveis com teto declarado — frequência ≥1 mudança/dia, amplitude ≥25% médio, velocidade = fração de mudanças na segunda metade calendárica da janela, persistência = deriva líquida/movimento total absoluto — score = média dos 4, 5 classificações). `FreshnessEngine` (Epic 4, compute-on-read, thresholds literais do brief — Live <5min/Fresh <1h/Recent <6h/Old <24h/Stale ≥24h — score interpolado entre âncoras, nunca dois conceitos desacoplados). `StoreUpdateIntelligenceService` (Epic 5, compute-on-read a partir de `market_changes`: intervalo médio entre eventos de sync — clusterizados por janela de 2min para não inflar por lote —, estabilidade de catálogo — 1 menos taxa de churn —, e "Price Reaction Speed" real: mediana do tempo entre a mudança de preço desta loja e a mudança mais próxima anterior no mesmo produto em outra loja, num lookback de 7 dias). `MarketPulseService` + `LiveActivityFeedService` (Epic 6/7, respondem as 9 perguntas do brief — contagens exatas via `COUNT` indexado, breakdown por categoria/loja/top movers via amostra limitada de linhas agregada em JS, ver TECH_DEBT). `BuyerAlertEngine` (Epic 8, puro — classifica só os 4 gatilhos concretos do brief: queda de preço, promoção, estoque voltou, produto novo; `RelevantChange` fica na taxonomia mas nunca é emitido, sem mecanismo de watchlist do comprador ainda) + `BuyerAlertService`/cron que só populam `buyer_alert_candidates` com `status='pending'` — **nenhum envio existe nesta Wave**, exatamente como o brief exige. Brain (Epic 9): 10 novos `TrustEventType` (`PriceDropped`/`PriceRaised`/`StockReturned`/`StockOut`/`ProductCreated`/`PromotionDetected`/`StoreHighlyResponsive`/`HighVolatilityDetected`/`LowVolatilityDetected`/`MarketTrendDetected`) — **todos taxonomia apenas nesta Wave**: toda mudança de mercado é por loja/produto, sem `merchantId` garantido (a maioria das lojas ainda não foi reivindicada), e resolver loja→merchant por evento de mudança individual não coube no escopo — mesma disciplina de `StoreDiscovered`/`MarketplaceHealthScoreChanged`. Dashboard `/admin/realtime-commerce` (Epic 10, 5 abas — Market Pulse/Live Activity/Top Movers/Lojas/Alertas —, mesmo padrão de composição `Promise.allSettled` de `/admin/exchange`), 5 rotas admin + 2 crons novos em `vercel.json` (`market-pulse` e `buyer-alerts`, ambos a cada 15 min). Epic 11/12 puramente documentação (fundação para Home Premium e para escala), sem UI pública nova e sem migração de arquitetura. Gaps documentados, não inventados (`docs/engineering/TECH_DEBT.md`): `ProductRemoved`/`OfferRemoved` não são detectados automaticamente (exigiria saber que um sync é exaustivo, o que nenhum conector declara hoje — implementar isso sem essa garantia arriscaria falso-positivar ofertas de um sync parcial como removidas); `CategoryChanged`/`BrandChanged` nunca disparam pela integração real porque `ExistingOfferLookup` não carrega categoria/marca anteriores (o detector suporta os dois, só falta um produtor que forneça o valor); breakdown de categorias/lojas do Market Pulse agrega em JS sobre uma amostra limitada (3000 linhas), não `GROUP BY` Postgres — ok para o volume atual, revisitar perto de centenas de lojas; `avgSyncTimeMs` do Store Update Intelligence é um parâmetro opcional não preenchido nesta Wave (exigiria cruzar `connector_sync_runs` por loja, decisão de manter o domínio sem essa dependência cruzada). Testes: 37 novos (`ChangeDetector`, `VolatilityEngine`, `FreshnessEngine`, `StoreUpdateEngine`, `BuyerAlertEngine` — todos puros, maior valor) — total 452/452. Quality Gate: lint 0, tsc 0, 452/452 testes, `db:lint` pendente de rodada final. **Migration requer `npm run db:push` pelo CTO**. **Deferido explicitamente**: detecção de remoção de produto/offer; emissão real dos 10 eventos Brain (depende de cobertura de lojas reivindicadas); `GROUP BY` real para Market Pulse em escala; wiring de `avgSyncTimeMs`.
>
> ✅ **Release 1.8 — Program A — Wave 1 — Exchange Intelligence Platform (2026-07-03)**: mandato do CTO — construir uma plataforma de inteligência cambial (10 Epics no brief original), não apenas um conversor. Desta vez a numeração de Program/Wave já batia com `docs/product/ROADMAP_1_8.md` (Program A / Wave 1 = Exchange Engine, sem colisão). Pré-voo encontrou dois pontos reais a resolver antes de escrever código: (1) o brief pedia um motor multi-provedor (Banco Central do Paraguai/Brasil, AwesomeAPI, fonte própria) mas a ADR-043 (já aceita) havia pesquisado e rejeitado explicitamente essas alternativas para a cadência de 5 minutos aprovada — **confirmado com o CTO**: constrói-se o mecanismo real de registro/failover, mas só o provedor da ADR-043 (ExchangeRate-API.com) é de fato registrado; (2) o Blueprint propunha uma coluna nova `original_currency` em `offers`, mas pesquisa de schema encontrou que `offers.currency` já existe com exatamente esse significado — reaproveitada em vez de duplicada (ADR-047). Terceiro ponto, confirmado com o CTO: construir para cadência `*/5 * * * *` no cron da Vercel agora, verificação do plano (Hobby vs. Pro, que suporta essa cadência) fica como ação separada, fora deste código. **Domínio novo** `src/domains/exchange/` (types/enums/providers/infrastructure/cache/repositories/history/services/analytics/events/dashboard/`__tests__`) — nunca depende de outro domínio (Epic 1); a ponte para o Brain (`TrustEventType`) vive em `lib/exchange-trust-bridge.ts`, fora do domínio, exatamente por essa regra. `ExchangeProviderRegistryImpl` (Epic 2, espelha `ConnectorRegistry`, ordena por prioridade) + `ExchangeRateApiProvider` (único provedor real) + `ExchangeRateApiHttpClient` (JSON fetch com timeout+retry/backoff, novo — `HttpFetchStrategy` existente não tinha retry e é HTML-shaped). `ExchangeRateService.refresh()` tenta provedores em ordem de prioridade, insere (nunca atualiza) linhas em `exchange_rates`, e degrada para a última cotação válida se todos falharem — nunca fabrica uma linha nova na falha (Anti-Pattern 5). `ExchangeRateCache` (Epic 3, TTL 60s em memória — "warm/cold" do brief colapsa numa camada só, o Postgres já é a "fria"). `ExchangeHistoryService` (Epic 4, `getRateAt` para consistência histórica). `AutomaticCurrencyService.convert()` (Epic 5, reaproveita `offers.currency`, nunca persiste nada, `rateVersion` referencia a linha exata de `exchange_rates` usada). `ExchangeAnalyticsService` (Epic 6, 5 métricas — variação cambial, velocidade de reação por loja e impacto por categoria explicitamente rotulados "proxy"/"simplificado" não causais, valorização de catálogo aproximada por reversão de deltas, economia do comprador). Brain (Epic 7): 5 novos `TrustEventType` — só `StoreRateReactionFast`/`Slow` (loja reivindicada, `merchantId` real) emitidos de verdade; os outros 3 (marketplace-wide) ficam taxonomia. Dashboard `/admin/exchange` (Epic 8, mesmo padrão de abas do Marketplace Operations). API pública `/api/exchange/{current,history,providers,health,convert}` (Epic 9, service-role-backed, mesmo padrão de `/api/canonical-catalog/[slug]`, `/providers` retorna campos reduzidos, sem texto de erro bruto) + `/api/admin/exchange/{overview,provider-runs}`. Cron `/api/cron/exchange/refresh` (novo, `vercel.json` a cada 5 min). Epic 10 puramente documentação (`docs/engineering/EXCHANGE_FOUNDATION_FOR_LIVE_PRICING.md`), sem código de Live Pricing. Gaps documentados, não inventados (`docs/engineering/TECH_DEBT.md`): terceira superfície pública sem rate limiting real; cache por instância serverless (inofensivo, DB já limita a defasagem); multi-provider é mecanismo com 1 provedor real; valorização de catálogo é aproximação, não snapshot diário; reaction-lag/category-impact são proxies; bug pré-existente do ShoppingChina (PYG gravado como `price_usd`) nomeado, não corrigido. Testes: 54 novos (formulas.ts — pura, maior valor —, ExchangeRateCache, ExchangeProviderRegistry, ExchangeRateApiProvider com fetch mockado, ExchangeHistoryService, ExchangeRateService com os 3 caminhos de failover, AutomaticCurrencyService, ExchangeProviderHealthService, event-registry) — total 415/415. Quality Gate: lint 0, tsc 0, 415/415 testes, build 174 rotas, `db:lint` OK (12 migrations). **Migration requer `npm run db:push` pelo CTO**. **Deferido explicitamente**: segundo provedor real (exige pesquisa própria + ADR); rate limiting real na API pública; correção do bug ShoppingChina; snapshot diário real de valor de catálogo.

> ✅ **Release 1.8 — Program 0 — Wave 1 — Marketplace Operations Platform (2026-07-03)**: mandato do CTO — construir o Centro de Operações do Marketplace (10 Epics no brief original). Pré-voo encontrou uma colisão de nomenclatura: `docs/product/ROADMAP_1_8.md` (Sprint Zero) já define Program A / Wave 1 como o Exchange Engine — este trabalho foi arquivado como **Program 0 — Wave 1**, continuando a trilha de infraestrutura/operações iniciada pelo Program 0 Wave 0 (Brain Analytics Integration), sem tocar a numeração de Program A. Escopo trimado deliberadamente (10 Epics do brief ≈ o dobro de uma Wave normal): Epics 1/2/3/5/6/8 construídos por completo; Epic 4 (Coverage) sem cobertura por segmento/preço (sem coluna real); Epic 7 (Dashboard) reduzido de 13 seções para 5 abas, linkando para páginas admin já existentes em vez de duplicá-las; Epic 9 (Brain) só taxonomia — sem novo `BrainAsset` (só 6 existem desde o Release 1.5, expandir é decisão própria fora do mandato desta Wave); Epic 10 é puramente documentação (`docs/engineering/MARKETPLACE_FOUNDATION_SCALE_AUDIT.md`), sem código. **Domínio novo** `src/domains/marketplace-operations/` (types/metrics/scoring/health/services/repositories/infrastructure/events/dashboard/`__tests__`) — `__tests__/` em vez do `tests/` literal do brief, correção deliberada do bug de descoberta do Jest já nomeado no Wave 0. `MarketplaceHealthEngine` (Epic 2, score 0-100, 8 fatores com peso documentado, `Promise.allSettled` isola falha de um fator). `MerchantPriorityService` (Epic 3, 100% compute-on-read — sem tabela própria, mesma lógica do merchant-decision: uma segunda fonte de verdade persistida arriscaria divergir do score ao vivo; Premium/SEO deliberadamente sem peso — não existem ainda). `MarketplaceCoverageService` (Epic 4). **Estende** (não duplica) o Connector Platform: `ConnectorHealthService` novo em `src/domains/connectors/services/` — `app/api/admin/monitor/summary/route.ts` (Ecosystem Monitor, Release 1.7 Wave 2) refatorado para delegar a ele em vez de inlinear o cálculo, uma implementação, dois consumidores. `MarketplaceMetricsService` (Epic 6). `MarketplaceAlertService` + `AlertRules` puras (Epic 8, 8 regras, dedupe-on-create por `(alert_type, subject_type, subject_id)`, lifecycle pending/acknowledged/resolved/ignored, mesmo padrão do `ActionService`/merchant-decision). `MarketplaceOperationsDashboardService` (Epic 7, mesma composição `Promise.allSettled` do `platform-health`). Migration `20260703100000_marketplace_operations.sql` (`marketplace_health_snapshots` — snapshot diário marketplace-wide, sem dimensão merchant_id, mesmo padrão de `merchant_catalog_snapshots`; `marketplace_alerts`). 10 rotas novas em `app/api/admin/marketplace-operations/*` + `GET /api/cron/marketplace-operations/snapshot` (novo cron diário em `vercel.json`, 07:00, após o sync de conectores às 06:00) que persiste o snapshot e roda a varredura de regras de alerta. `/admin/marketplace-operations` — primeira página `/admin` a usar o padrão de abas internas já estabelecido em `app/merchant/dashboard/page.tsx` (Overview/Cobertura/Conectores/Prioridade/Alertas), buscando o payload composto uma única vez em vez de um fetch por aba. Brain: 5 novos `TrustEventType` — só `MerchantPriorityTierChanged` (tem `merchantId` real, por loja) ganhou factory function; os outros 4 (marketplace-wide, sem merchant natural) ficam taxonomia, mesma disciplina do `StoreDiscovered`. **Não emitido nesta Wave**: `MerchantPriorityTierChanged` — detectar uma mudança real de tier exige saber o tier anterior, e `MerchantPriorityService` é deliberadamente compute-on-read sem histórico; plumbing pronto, emissão adiada (mesma postura do Product Identity Engine em Shadow Mode). Gaps documentados, não inventados (`docs/engineering/TECH_DEBT.md`): sem coluna de segmento em `stores`; sem premium/SEO real; sem contagem de retries; "latência" é duração de execução, não latência por requisição; sem histórico de estoque; Knowledge Graph não agregado marketplace-wide (`knowledgeRelations: null`, não zero fabricado). Docs atualizados (não criados — já existiam desde o Sprint Zero): `MARKETPLACE_STRATEGY.md`, `MARKETPLACE_VISION.md`, `KPIS.md`, `ROADMAP_1_8.md` (nova seção PROGRAM 0), `RELEASE_1_8_BLUEPRINT.md` (nota de cross-reference). Testes: 64 novos (HealthScoring/PriorityScoring/CoverageScoring/AlertRules — pura, maior valor —, ConnectorHealthService, MarketplaceHealthEngine, MerchantPriorityService, MarketplaceAlertService, MarketplaceSnapshotService, event-registry) — total 361/361. Quality Gate: lint 0, tsc 0, 361/361 testes, build 165 rotas, `db:lint` OK (11 migrations). **Migration requer `npm run db:push` pelo CTO** (padrão de todo Wave anterior). **Deferido explicitamente**: emissão real de `MerchantPriorityTierChanged`; agregação por categoria/marca via `GROUP BY` Postgres em vez de JS (necessário perto de 500 mil produtos, ver auditoria de escala); novo `BrainAsset` para "Marketplace Intelligence" (decisão própria, precisa de ADR).

> ✅ **Release 1.8 — Program 0 — Wave 0 — Brain Analytics Integration (2026-07-02)**: constrói a ponte oficial entre `buyer_events` e o Brain, nomeada como gap real no Sprint 0.1. Novo `BuyerEventBrainBridgeService` roda sincronamente a cada `buyer_events` gravado com `merchant_id` resolvido — sem fila, sem cron — convertendo em `merchant_trust_events` (`MerchantViewed`, `MerchantPassportViewed`, cliques de contato consolidados em `MerchantContactClicked`). Identidade sempre pseudônima, nunca em `created_by` (FK para `profiles`, compradores nunca são linhas de `profiles`). `brain_synced_at`/`brain_sync_error` (nova migration) garantem execução única. **Dois bugs reais encontrados e corrigidos** — expostos porque esta foi a primeira vez que `CognitiveBrainService`/`KnowledgeGraphService` (ambos existentes desde o Release 1.5, só chamados por testes até agora) foram chamados por código de produção: `CognitiveBrainService.ingest()` encaminhava `actor_id` para `created_by` incondicionalmente, violando a FK para todo comprador; `KnowledgeGraphService` lia `created_by` (nunca populado para compradores) em vez de `metadata.buyer_pseudonym`. Nova rota `GET /api/trust/merchant/[merchantId]/graph` — primeira a expor o Knowledge Graph. Testado ponta a ponta contra produção real (evento → Brain event → relação derivada → consulta, incluindo acúmulo através de duas visitas), dado sintético removido após confirmação. Achado colateral não corrigido: `src/domains/trust/tests/*.test.ts` nunca são descobertos pelo Jest (fora de `__tests__/`). Limitação real e nomeada: zero lojas reivindicadas em produção hoje, então o volume real processado ainda é zero — funil de produto, não bug de engenharia; sinais sem merchant (busca, navegação) continuam sem destino no Brain, decisão de arquitetura própria fora do escopo desta Wave. Detalhamento completo: `docs/product/releases/RELEASE_1_8_PROGRAM_0_WAVE_0_REPORT.md`. Quality Gate: lint 0, tsc 0, 297/297 testes, build sucesso, `db:lint` OK.

> ✅ **Release 1.8 — Sprint 0.1 — Critical Readiness Fixes (2026-07-02)**: resolve as 2 inconsistências críticas do Sprint Zero. Investigar por que `useAnalytics` nunca era chamado revelou que **as migrations `0018`-`0021` (todo o Release 1.6 a partir do Epic 2 — Analytics, Decision Engine, Catalog Intelligence, Growth Engine) nunca haviam sido aplicadas em produção**, apesar de documentadas como entregues — confirmado por query direta contra `information_schema.tables`, não suposição. Aplicadas via `supabase db push` nesta data; todas as 6 tabelas confirmadas existindo. `useAnalytics` conectado com cobertura mínima (`BuyerSessionTracker` global + 3 trackers de página) — evidência de ponta a ponta demonstrada (evento real inserido via API, confirmado por query, depois removido). Achado novo: o Brain não recebe nenhum `buyer_events` (zero bridge de código entre `merchant-analytics` e `trust`) — não corrigido, é arquitetura nova, fora do mandato deste Sprint. Canonical Route Audit: `/lojas/[slug]` definida URL canônica única de loja; `/store/[slug]` removida, redirect 308 permanente verificado em runtime real (`curl`); sitemap corrigido sem reduzir cobertura (armadilha real evitada: o case removido cobria todas as lojas, o que sobrou cobria só top-100 — ajustado antes de virar regressão silenciosa); `StoreDetails.tsx` (órfão pela remoção) também removido. Detalhamento completo: `docs/product/releases/RELEASE_1_8_SPRINT_01_REPORT.md`. Quality Gate: lint 0, tsc 0, 281/281 testes, build sucesso, `db:lint` OK.
>
> ✅ **Pré-Release 1.8 — Buyer Identity Model (ADR-046, 2026-07-02)**: última decisão arquitetural pendente antes do início de qualquer Wave do Release 1.8 relacionada a comprador. Detalhamento completo em `docs/product/releases/RELEASE_1_8_BUYER_IDENTITY_MODEL.md` (15 seções). Decisão: `buyers` como aggregate root de domínio próprio (`src/domains/buyer-identity/`, ainda não criado — arquitetura apenas, sem código) — nunca reaproveita `profiles` (staff/operator/merchant, por decisão deliberada da ADR-031, Release 1.2) nem referencia `auth.users` diretamente fora do próprio domínio. Ciclo de vida de 6 estados (Anônimo → Sessão Anônima → Conhecido [não verificado/verificado] → Autenticado → Recorrente [rótulo derivado, não uma transição real] → Premium [futuro]) mais um estado terminal de Anonimização (LGPD, sem destruir sinal comportamental agregado). Auditoria de acoplamento (não apenas do schema de Buyer, mas de `profiles`/`auth.users`/`merchant_reviews`/`buyer_events`/`buyer_sessions`/Trust/Ownership/Growth/Canonical Catalog) encontrou 3 dívidas pré-existentes — nenhuma corrigida, todas nomeadas para a Wave 6: `handle_new_user()` rotula todo novo cadastro como `'operator'`; `buyer_events`/`buyer_sessions.buyer_id` já referencia `auth.users` diretamente (Release 1.6), acoplamento a corrigir migrando para `buyers.id`; `merchant_reviews.reviewer_id → profiles` nunca foi de fato alimentado por um comprador real (gap latente do Release 1.5). Também encontrou **2 achados de segurança de severidade alta já em produção**, não causados por este trabalho: `buyer_events`/`buyer_sessions` aceitam INSERT anônimo irrestrito (o comentário do próprio SQL alega rate limiting que a auditoria de segurança da Wave 6 do Release 1.7 já provou não existir), e a coleta comportamental roda desde o Release 1.6 sem nenhum aviso de privacidade/consentimento — ambos recomendados para correção prioritária na Wave 6, não adiamento. Brain: vocabulário `BrainEntityType.Buyer`/relações `Buyer*` já reservado desde o Release 1.5 (Epic 4), nunca conectado até esta decisão definir o identificador que o preenche — pseudonimização via `buyers.id`, PII nunca acessível ao pipeline do Brain, controlada por RLS. Assets/Moats: nenhum novo — matura diretamente `C-6 Buyer Behavioral Knowledge` (Incipiente → Ativo Maduro) e acelera Moat 6 (Data Flywheel)/Moat 7 (Brain), já catalogados. Um parâmetro fica deliberadamente em aberto (prazo de retenção de conta inativa — decisão jurídica/de negócio, não técnica). **Nenhum código, migration ou alteração de banco foi feito** — conforme mandato explícito do CTO.

> ✅ **`npm run db:push` executado pelo CTO (2026-07-02)**: `npx supabase db push --dry-run` e `npx supabase db push` (real) confirmam **"Remote database is up to date"** — as 5 migrations em `supabase/migrations/` (`connector_platform`, `merchant_entitlements_discovery`, `product_identity`, `canonical_catalog`, `merchant_ownership` — antigas `0022`-`0026`) já estavam aplicadas ao projeto Supabase live antes deste comando rodar (`npx supabase migration list` confirma `local`==`remote` para as 5). Fecha a única pendência operacional apontada pela certificação da Wave 6 (`RELEASE_CERTIFICATION_1.7.md` Capítulo 17). Connector Platform, Product Identity, Canonical Catalog e Merchant Ownership estão, portanto, de fato ativos no banco de produção, não apenas no código.
>
> ✅ **Release 1.7 — Wave 6 — Platform Hardening, Certification & Release Lock (2026-07-02)**: Sexta e última entrega do Release 1.7 — mandato do CTO: auditoria completa da plataforma, correção de achados crítico/médio, sem domínio novo/regra de negócio nova/feature grande. Relatório executivo completo em `docs/operations/RELEASE_CERTIFICATION_1.7.md`. **Achado crítico corrigido**: `ProgressiveVerificationEngine` (Wave 5) auto-aprovava claims usando sinais (telefone/WhatsApp/website/Instagram) todos publicamente visíveis em `/lojas/[slug]` — qualquer conta autenticada podia auto-aprovar uma claim fraudulenta copiando dados da própria página pública da loja. `ClaimService.create` desliga a auto-aprovação por completo (`AUTO_APPROVAL_ENABLED = false`); toda claim roteia para revisão humana; confidence/evidência seguem persistidas. Ver ADR-042. **Achado médio corrigido**: `DelegationService.accept` não checava se o e-mail da sessão autenticada batia com o e-mail do convite — um token vazado bastava para assumir uma vaga de delegado; agora exige `acceptingUserEmail` resolvido só no servidor. **Achados baixos corrigidos**: `lib/cron-auth.ts` passa a usar `crypto.timingSafeEqual` (era `!==`, vulnerável a timing attack); 33 `DROP POLICY IF EXISTS` adicionadas em 4 migrations pré-`0017` sem guard de idempotência. **Bug de dados corrigido**: 4 leituras de `import_logs` (tabela congelada desde a Wave 2) repontadas para `connector_sync_runs` (`app/admin/page.tsx`, dashboard stats, Catalog Intelligence e Executive Summary do merchant Command Center — todos mostravam "nunca importado" para lojas sincronizadas via Connector Platform). **SEO**: `sitemap.xml` monolítico substituído por sitemap-index via `generateSitemaps()` (3 shards estáticos + shards de produto de 20k slugs paginados, sem limite implícito de ~25k produtos). **Next.js 16**: `middleware.ts`→`proxy.ts` (deprecação oficial do framework, sem dependência de Edge runtime). **Performance**: dedup de fetch em `app/lojas/[slug]/page.tsx` via `React.cache()`. **Observabilidade**: `GET /api/admin/platform-health` consolida 7 domínios num único endpoint admin. Arquitetura/DDD auditados e confirmados limpos (zero violação de direção de dependência, zero domínio duplicado/morto, zero dependência circular). RLS auditada em todas as 9 tabelas novas das Waves 1-5 — 100% com RLS habilitada, 8/9 corretamente sem policy pública. Todas as ~30 rotas admin confirmadas com `requireAdmin()`. Achados documentados sem correção nesta Wave (fora do mandato "sem features"): rate limiting ausente em endpoints de mutação (ADR-042 parte 3); `proxy.ts` não cobre rotas de API (seguro hoje, toda rota se autoguarda); 5 serviços pré-Wave 4/5 com `supabase.from()` embutido em vez de repositório (dívida de padrão). Testes: 2 novos — total 281/281. Quality Gate: lint 0, typecheck 0, 281/281 testes, build 157 rotas, `db:lint` OK. **RELEASE 1.7 CERTIFICADO E CONGELADO** — evolução futura exclusivamente no Release 1.8.
>
> ✅ **Release 1.7 — Wave 5 — Merchant Acquisition & Ownership Platform (2026-07-01)**: Quinta entrega do Release 1.7 — segundo re-escopo do CTO: "Merchant Claim + Onboarding" (2 bullets) virou uma plataforma completa de aquisição (8 Epics), com prioridade explícita de crescimento de negócio sobre infraestrutura (`RELEASE_1_7_BLUEPRINT.md` v1.3). Migration `supabase/migrations/20260701120400_merchant_ownership.sql` (`store_claims`, `merchant_delegates`, `merchant_upgrade_leads`, extensão de `merchant_verifications` com o tipo `store_claim`). Domínio novo `src/domains/merchant-ownership/` — depende de `trust/` deliberadamente, reaproveitando `VerificationService`/`VerificationEvidenceService`/`EventService` já existentes desde o Release 1.5 em vez de duplicar a máquina de verificação. `ProgressiveVerificationEngine` (puro, explicável — compara e-mail/telefone/WhatsApp/website/Instagram submetidos contra os já cadastrados na loja; sem chamadas a Meta Graph API/WhatsApp Business API, sem credenciais neste ambiente; gate anti-fraude estrutural — confidence 0 nunca auto-aprova, testado explicitamente). `ClaimService` (create idempotente com auto-aprovação quando a confiança é alta, approve/reject/requestInfo/revoke — revogação nunca apaga o histórico da claim). `DelegationService` (invite/accept/revoke com `actingRole` explícito — só o proprietário convida/revoga, reforçado no serviço, não só na rota). `PremiumUpgradeService` (lead-capture, ADR-035, sem billing). `OwnershipLevelService` (nível computado sob demanda: StoreDiscovered→ClaimRequested→IdentityVerified→OwnershipVerified→MerchantVerified→PremiumMerchant, sem coluna nova). Auth: `requireMerchantContext()` novo e aditivo em `lib/merchant-auth.ts` (resolve proprietário ou delegado ativo) — `requireMerchant()` existente intocado, as ~15 rotas de merchant já existentes não mudaram. UI real (diferente do precedente backend-only das Waves 3/4, confirmado com o CTO): `app/merchant/claim/[storeSlug]` (Smart Claim Flow), `ClaimStoreButton` em `/store/[slug]`/`/lojas/[slug]` (via novo `isUnclaimed`/`isStoreUnclaimed()`), `/admin/claims`+`/admin/claims/[id]` (Claim Review Center — real, com breakdown de sinais/evidências/histórico/eventos do Brain/trust signals), `DelegatesSection` em `/merchant/settings` (convite/lista/revogação mínima), `WelcomeBanner` do dashboard estendido com números reais, badge "+N Premium" e card de plano agora clicáveis (upgrade-interest). Brain: 10 novos `TrustEventType` — **8 com emissão real** (`ClaimRequested`, `ClaimCancelled`, `OwnershipVerified`/`Rejected`/`Revoked`, `ManagerInvited`/`Accepted`, `PremiumUpgradeViewed`), diferente de toda Wave anterior — uma claim/delegação/interesse de upgrade sempre tem `merchantId`. Só `PremiumTrialStarted`/`PremiumActivated` ficam taxonomia (sem trial/billing real). Strategic Assets/Moats atualizados (C-2/C-6/S-1, Moat 2/3/6/7). Testes: 34 novos — total 279/279. Quality Gate: lint 0, typecheck 0, 279/279 testes, build 152 rotas (+11), `db:lint` OK. **Migration `0026` requer `npm run db:push` pelo CTO.** **Deferido explicitamente**: retrofitting de `requireMerchantContext()` nas rotas de merchant existentes; onboarding pós-claim guiado além do já existente; fila de processamento real (sem infraestrutura de fila neste projeto, decisão confirmada); emissão real de `PremiumTrialStarted`/`PremiumActivated`; verificação via APIs de terceiros (Meta/WhatsApp Business); checagem de Facebook em Progressive Verification (sem coluna `stores.facebook`).
>
> ✅ **Release 1.7 — Wave 4 — Canonical Catalog & Compare Foundation (2026-07-01)**: Quarta entrega do Release 1.7 — re-escopo do CTO: a Wave 4 original ("Merchant Claim + Onboarding") foi realocada para a nova Wave 5, dando lugar ao Canonical Catalog — a fundação de identidade permanente de produto de que Compare/Search/Recommendation Engine/Merchant Intelligence/Brain dependerão (`RELEASE_1_7_BLUEPRINT.md` v1.2). Mission: nenhuma URL quebrada, nenhum Product removido, nenhuma Offer perde histórico. Migration `supabase/migrations/20260701120300_canonical_catalog.sql` (`canonical_products` com `canonical_slug` único/imutável; `offers.canonical_product_id` nova coluna nullable — `offers.product_id` nunca tocada; `merge_candidates` com explainability completa: confidence/algorithm_version/matched_attributes/mismatched_attributes/penalties/reason/status/reviewed_at/reviewed_by). Domínio novo `src/domains/canonical-catalog/` — **domínio fundação**: nunca depende de `connectors/` nem `product-identity/`, todos os outros domínios podem depender dele. `CanonicalProductService` (`bootstrapFromProduct` idempotente, 1:1 e sem perda — reaproveita o slug já único do produto existente, não é uma união; `generateCanonicalSlug` com resolução de colisão para canonical products futuros), `OfferRankingService` (preço/estoque/recência/confiança verificável/qualidade — **nunca Reputation Score**, restrição permanente do Release 1.5; fator "trust" é `stores.is_verified` explícito, resolvido pelo chamador), `CanonicalPriceHistoryService` (agregação sob demanda de `price_history` por canonical product, sem tabela nova — lowest/highest/average/variação/tendência), `CompareFoundationService` (compõe os três — a infraestrutura real do Compare Foundation, backend/API apenas nesta Wave, sem página nova). Repositórios (`ICanonicalCatalogRepository`/`IMergeCandidateRepository`/`ICanonicalPriceHistoryRepository` + Supabase) — o de merge candidates deliberadamente sem método de execução de união, mesma garantia estrutural (não convencional) de Shadow Mode já usada na Wave 3. Extensão em `product-identity/`: `CanonicalMergeSuggestionService` (único código que depende de `canonical-catalog/`, nunca o contrário — reaproveita o `ProductIdentityEngine` já existente para avaliar pares de canonical products da mesma marca, escreve `MergeCandidate`s no tier `possible`+). `scripts/canonical-catalog-bootstrap.ts` (dry-run por padrão, bootstrap 1:1 + seed de merge suggestions) e `lib/canonical-catalog-factory.ts` (wiring, novo). APIs backend/API apenas (sem UI, confirmado com o CTO): `GET/PATCH /api/admin/canonical-catalog/merge-candidates[/[id]]` (Match Review — aprovar/rejeitar/ignorar grava só a decisão humana, nunca reatribui offers), `GET /api/canonical-catalog/[slug]` (Compare Foundation, service role interno, mesmo padrão de `/lojas`/ADR-036). Brain: 10 novos `TrustEventType` (taxonomia apenas — `CanonicalProductCreated`, `OfferLinked`/`OfferUnlinked`, `MergeSuggested`/`Approved`/`Rejected`, `CanonicalViewed`, `CompareViewed`, `PriceHistoryViewed`, `LowestPriceReached`), nenhum emitido ainda. **Achado não relacionado**: teste de completude descobriu 21 `TrustEventType` do Release 1.6 sem mapeamento no Brain — registrado em `TECH_DEBT.md`, não corrigido (fora do escopo desta Wave). Strategic Assets/Moats atualizados (C-1/C-3/C-4/C-5, Moat 1/4/5/6/7). Testes: 26 novos — total 245/245. Quality Gate: lint 0, typecheck 0, 245/245 testes, build OK, `db:lint` OK. **Migration `0025` requer `npm run db:push` pelo CTO. Bootstrap (`--execute`) não executado nesta sessão** — grava no projeto real, fica para o CTO rodar após a migration. **Deferido explicitamente**: execução real de merges aprovados; UI de Match Review; rota pública `/produto/[slug]`; wiring do bootstrap/merge-suggestion no `SyncOrchestrator`; emissão real dos 10 eventos Brain.
>
> ✅ **Database Migration System V2 (2026-07-01)**: Mandato do CTO — elimina o fluxo de aplicação manual de migrations via Supabase SQL Editor como caminho padrão, superando parcialmente ADR-017/018 (ver ADR-040 em `docs/operations/DECISIONS.md`). Auditoria (ETAPA 1) encontrou um bug real em 4 migrations (`0018`-`0021`): a verificação embutida usava `information_schema.tables.row_security`, coluna que não existe em PostgreSQL — corrigido para `pg_tables.rowsecurity`, e extraído para `database/verification/00NN_verify.sql` (nunca mais embutido em uma migration). `0018` ganhou `DROP POLICY IF EXISTS` antes de suas policies (idempotência — mesma classe de bug que gerou `0017_hotfix_trust_experience.sql`). Nenhuma dessas 4 edições reaplica nada em produção (já estavam aplicadas) — é higiene de arquivo. `0022`-`0024` (nunca aplicadas) foram movidas para `supabase/migrations/` com nomes no formato exigido pela Supabase CLI — a partir de agora, todo schema novo nasce ali via `npx supabase migration new`, aplicado por `npm run db:push`, nunca mais copiado à mão. Novo: `database/verification/` (7 arquivos), `database/health_checks/` (7 arquivos por tópico — rls/policies/indexes/foreign_keys/triggers/extensions/storage_buckets, independentes de qualquer migration), `database/templates/` (MIGRATION/VERIFICATION/ROLLBACK, com classe de rollback obrigatória: Possible/Partial/Impossible), `supabase/config.toml` (gerado via `supabase init` real, CLI v2.109.0) + `supabase/seed.sql` (placeholder — seeding de catálogo continua em `database/seed/*.js`, intocado), `scripts/lib/migration-lint.ts`+`scripts/db-migration-lint.ts` (valida programaticamente que nenhuma migration contém SELECT avulso — 6 testes novos), `scripts/db-verify.ts` (lista verificações disponíveis). `package.json`: `db:push`/`db:reset`/`db:diff`/`db:status`/`db:lint`/`db:verify` via Supabase CLI (nova devDependency `^2.109.0`), sem colidir com `db:seed`/`db:seed:execute` existentes. `.github/workflows/database.yml` (novo, dormant até secrets serem configurados): lint → typecheck → test → db:lint → supabase db push → health checks — documentado explicitamente que não existe projeto de staging separado ainda. Docs: `docs/engineering/DATABASE_ENGINEERING.md` (novo, padrão autoritativo + runbook do CTO), pointers em `ENGINEERING_PRINCIPLES.md`/`CONVENTIONS.md`/`RELEASE_STRATEGY.md`. Testes: 6 novos — total 219/219. Quality Gate: lint 0, typecheck 0, 219/219 testes, build 138 rotas (nenhuma rota nova). **Passo manual restante**: o CTO precisa rodar `supabase login` + `supabase link --project-ref acairzpzsklctaqjsukw` + `npm run db:push` uma única vez (runbook completo em `DATABASE_ENGINEERING.md` §9) — esta sessão não tem, e não deveria ter, as credenciais de produção necessárias para isso.
>
> ✅ **Release 1.7 — Wave 3 — Product Identity Engine (Shadow Mode) (2026-07-01)**: Terceira entrega do Release 1.7. Product Identity promovido a domínio próprio (`src/domains/product-identity/`) — Core Asset permanente (`RELEASE_1_7_BLUEPRINT.md` Capítulo 8), independente de `src/domains/connectors/` (que passa a depender dele, nunca o contrário). CTO aprovou, antes da implementação, duas decisões permanentes: **postura conservadora** de matching (falso positivo — unir dois produtos diferentes — é inaceitável; falso negativo é aceitável e corrigível depois) e **rollout em Shadow Mode** (o motor avalia e registra candidato/confidence/fatores/decisão sugerida, mas não altera o catálogo real nesta Wave — nenhum slug muda, nenhum Product é fundido, nenhuma Offer é remanejada). Migration `0024_product_identity.sql` (tabela `product_identity_match_log`, RLS habilitado, sem policy pública). Domínio completo: `ProductIdentityEngine` (determinístico, sem ML/embeddings — brand/category como gates que limitam a confidence a 40 se não baterem; score por similaridade de nome via Jaccard de tokens, sobreposição de especificações, tokens de modelo/capacidade — cada fator registrado com evidência, nunca um número opaco), 4 tiers nomeados (`auto` 95–100 / `probable` 85–94 / `possible` 70–84 / `new_product` <70), `IProductCandidateRepository`+`SupabaseProductCandidateRepository` (leitura de candidatos por marca, deliberadamente independente de `ICatalogRepository` — mantém o domínio reutilizável por busca/comparação/recomendações futuras), `IProductIdentityMatchLogRepository`+`SupabaseProductIdentityMatchLogRepository` (fire-and-forget, mesma convenção de `insertPriceHistory`, sem método de update — append-only por código), `ProductIdentityService` (orquestração, nunca lança exceção). **Explainability completa (revisão do CTO, pré-commit)**: `MatchResult`/`MatchLogEntry` carregam `algorithmVersion` (constante `PRODUCT_IDENTITY_ALGORITHM_VERSION`, hoje `"1.0.0"` — futuras evoluções do algoritmo incrementam essa versão; avaliações históricas nunca são recalculadas em lugar), `matchedAttributes`/`mismatchedAttributes` (um item por atributo avaliado), `penalties` (todo ponto não concedido rastreado a um atributo e uma razão, incluindo uma entrada dedicada para o cap do gate marca/categoria) e `explainabilityReason` (frase legível sintetizando a decisão) — dado Brain-facing, ativo estratégico permanente, não apenas debugging. Em `connectors/`: `ProductIdentityShadowStage` (novo estágio, entre `DeduplicationStage` e `CatalogWriteStage`, só avalia itens `"new"`, dupla camada de proteção contra exceções — nunca toca `ctx.deduplicated`/`ctx.persisted`); `ProductIdentityResolver`/`domain/ProductIdentity.ts` (stub do Epic 1) removidos. **Change Detection** (fecha gap do Epic 1): `DeduplicationStage` compara preço + estoque (`in_stock`/`stock_quantity`) + descrição + imagem, não mais só preço; `ICatalogRepository.findOfferByProductAndStore`/`ExistingOfferLookup` estendidos com os campos necessários. **Correção de `price_history`** (fecha outro gap do Epic 1): `CatalogWriteStage` grava histórico também no caminho `"new"`, não só `"update"` — todo produto acumula patrimônio temporal desde a primeira sincronização. Brain: **nenhum `TrustEventType` novo** — nenhum merge acontece de verdade ainda; a trilha de auditoria explicável é a preparação "pronta para o Brain" que o Quality Gate da aprovação exigia; emissão de eventos reais de merge fica para a Wave 5. Testes: 11 novos (`ProductIdentityEngine` ×5, `ProductIdentityService` ×3, `DeduplicationStage` ×3 de change detection) — total 213/213. Quality Gate: lint 0, typecheck 0, 213/213 testes, build 138 rotas (nenhuma rota nova — sem UI/API nesta Wave, decisão confirmada com o CTO). **Migration 0024 requer aplicação manual pelo CTO no Supabase SQL Editor.** **Deferido explicitamente**: UI/API de inspeção do shadow log; emissão de eventos Brain para merges reais; qualquer promoção de sugestões do Shadow Mode para merges automáticos (exige nova aprovação do CTO); embeddings/IA no motor de matching.
>
> ✅ **Release 1.7 — Wave 2 — Merchant Connectors + Scheduler + Discovery (2026-07-01)**: Segunda entrega do Release 1.7, após o CTO reorganizar as Epics 2–7 remanescentes em 4 Waves (`docs/product/releases/RELEASE_1_7_BLUEPRINT.md` v1.1) e declarar Product Identity um Core Asset permanente (Capítulo 8 do Blueprint — a ser promovido para `src/domains/product-identity/` na Wave 3). Migration `0023_merchant_entitlements_discovery.sql` (`stores.discovered_at`/`discovery_connector_key` — apenas proveniência, nenhuma coluna de ownership; `import_logs` marcada superada). **Ownership + Entitlements**: `services/merchant.service.ts` ganhou `getMerchantStoreIds()`, `merchantOwnsStoreSlug()`, `checkImportEntitlement()` (lê `merchant_plans.has_connectors`/`max_imports_month`, conta execuções não-dry-run em `connector_sync_runs` no mês); `POST /api/merchant/imports/run` fecha a lacuna de autorização do Epic 1 (403 se o merchant não for dono da loja do conector, ou se o plano/cota bloquear). `getMerchantDashboardStats()` migrada para `connector_sync_runs` (corrige um bug real: a query antiga de `import_logs` não era filtrada por merchant). **`import_logs` superada por completo**: escrita dupla removida das rotas de import; `/admin/logs` e `/merchant/imports` repontados para `connector_sync_runs` via `lib/sync-run-mapper.ts` — zero mudança de UI. **Scheduler**: primeiro cron deste projeto (`vercel.json`, diário), primeiro auth por segredo compartilhado (`lib/cron-auth.ts`, `CRON_SECRET`), `app/api/cron/connectors/sync/route.ts` (interval-based via `connectors.config.syncFrequencyHours`, opt-in, sem parser de cron real — decisão confirmada), `VercelCronScheduler` preenche o seam `ISyncScheduler` do Epic 1. **Ecosystem Monitor**: `/admin/monitor` (novo, nav "Ecossistema"), computado sob demanda a partir de `connector_sync_runs` (sem tabela de agregação nova). **Discovery** (`src/domains/connectors/discovery/`): `SitemapParser`/`RobotsParser` hand-rolled (sem nova dependência), `SitemapDiscoverySource` (não `IConnector` — produz uma loja candidata, não ofertas), `DiscoveryService` (único caminho autorizado a criar `stores` sem uma já existir, bypassando `ICatalogRepository` deliberadamente para preservar a garantia do `CatalogWriteStage` para conectores normais); `POST /api/admin/discovery/run`, admin, um domínio por vez — **decisão confirmada com o CTO**: sem seed-list nem sweep automático nesta Wave. Brain: 3 novos `TrustEventType` (`ConnectorSyncScheduled`, `ConnectorSyncSkippedEntitlement`, `StoreDiscovered` — este último apenas taxonomia, não ingerido, pois nenhum merchant existe no momento da descoberta; nome escolhido — não `MerchantDiscovered` — por decisão confirmada com o CTO). Testes: 23 novos (total 202/202). Quality Gate: lint 0, typecheck 0, 202/202 testes, build 138 rotas. **Migration 0023 requer aplicação manual pelo CTO no Supabase SQL Editor.** **Deferido explicitamente**: refatoração dos outros 7 call sites de `merchant_stores` (cleanup opcional); admin UI para `syncFrequencyHours` (edição manual no Supabase); seed-list/sweep automático de Discovery; Product Identity real (Wave 3).
>
> ✅ **Release 1.7 — Epic 1 — Connector Platform Framework (2026-07-01)**: Primeiro Epic do Release 1.7 ("Ecosystem Expansion Platform") entregue. Absorve por completo o `acquisition/` (Release 0.9) em um domínio DDD (`src/domains/connectors/`), corrigindo o vazamento de infraestrutura em que `PipelineContext` carregava um `SupabaseClient` bruto (agora `ICatalogRepository`). Blueprint completo em `docs/product/releases/RELEASE_1_7_BLUEPRINT.md` (13 capacidades A–M mapeadas em 7 Epics faseados, cada um com seu próprio Quality Gate — desvio deliberado do ritmo de entrega única dos Releases 1.5/1.6, justificado pelo tamanho deste Release). Migration `0022_connector_platform.sql` (tabelas `connectors` — registro persistente, substitui o `ConnectorRegistry` apenas-em-memória — e `connector_sync_runs` — execução de sincronização com `merchant_id` opcional e status `running/success/partial/failed`; `connector_configs` da migration 0010 marcada como superada, não removida). Domínio completo: `types/`, `domain/` (Connector, SyncRun, ProductIdentity — seed do Epic 3), `repositories/`+`infrastructure/` (IConnectorRepository, ISyncRunRepository, ICatalogRepository com implementações Supabase), `normalization/` (OfferNormalizer, ProductIdentityResolver), `mapping/` (JsonFieldMapper, CsvFieldMapper), `crawler/` (HttpFetchStrategy, ShoppingChinaConnector, reference JSON/CSV connectors — auto-registro agora consistente entre todos os conectores), `services/` (ConnectorRegistry, SyncOrchestrator substitui AcquisitionPipeline, 5 stages: Validation/Normalization/Deduplication/Media/CatalogWrite), `scheduler/` (ISyncScheduler — interface-only, seam para o Vercel Cron real do Epic 2; ManualSyncTrigger — o único trigger real do Epic 1), `events/connector.events.ts`. Factory `lib/connectors-factory.ts`. Brain: 4 novos `TrustEventType` (`ConnectorRegistered`, `ConnectorSyncStarted/Completed/Failed`) — **primeiro domínio fora de `src/domains/trust/` a alimentar o Brain** — mapeados para `BrainAsset.HistoricalData`/`SearchIntelligence`, mas emitidos **apenas no caminho de sincronização disparado por merchant** (`merchantId` presente); o caminho admin/global não emite eventos nesta Epic — limitação deliberada, documentada, não um bug. 4 rotas de API rewired sem mudança de contrato (`GET /api/admin/import/connectors`, `POST /api/admin/import/run`, `POST /api/merchant/imports/run`, `GET /api/merchant/imports/history`) — UIs `app/admin/imports/*`/`app/merchant/imports/*` inalteradas. `import_logs` recebe escrita dupla temporária (removida no Epic 2, quando `getMerchantDashboardStats()` migra para `connector_sync_runs`). Scripts CLI movidos de `acquisition/scripts/` para `scripts/` (`connectors:import-json[:execute]`, `connectors:import-csv[:execute]`, `sync:shoppingchina[:execute]`); `npm run acquisition:validate` removido, substituído pela suíte Jest. **Paridade verificada ao vivo**: dry-run de `scripts/import-json.ts`/`import-csv.ts` contra o Supabase real produziu totais idênticos (Received/Validated/Normalized/Deduplicated/Persisted/Skipped/Failed) aos scripts antigos de `acquisition/`, antes de sua remoção. Testes: 38 novos (9 suítes: ValidationStage, OfferNormalizer, JsonFieldMapper, CsvFieldMapper, ConnectorRegistry, SyncOrchestrator, CatalogWriteStage, DeduplicationStage, HttpFetchStrategy) — total 179/179 no repositório. Quality Gate: lint 0, typecheck 0, 179/179 testes, build 89 rotas. **Lacunas conhecidas, preservadas e sinalizadas explicitamente para Epic 2** (não corrigidas nesta Epic): `requireMerchant()` não verifica `merchant_stores` antes de permitir disparar um conector (`// TODO(Epic 2)` no código); `DeduplicationStage` só compara `price_usd` (mudanças de estoque/descrição/imagem são invisíveis — Epic 3); ofertas novas nunca recebem linha em `price_history` (só o caminho de atualização grava — Epic 3); nenhum scheduler real existe ainda (`ISyncScheduler` é apenas a interface). **Migration 0022 requer aplicação manual pelo CTO no Supabase SQL Editor antes de qualquer escrita real via o novo domínio** — sem ela, `connectorRepo`/`syncRunRepo` degradam graciosamente (retornam `null`, logam erro), mas nenhum registro de conector/sincronização é persistido.
>
> ✅ **Release 1.6 — Epic 5 — Growth Engine — Sprint 1.6.5 (2026-06-30)**: Quinto e último Epic do Release 1.6 entregue. Motor de crescimento que responde "O que devo fazer hoje para vender mais?" de forma prática, explicável e recorrente. Completamente baseado em regras (zero IA generativa) — cada recomendação tem evidência, fonte de dados, impacto esperado e explicação do porquê. Migration `0021_growth_engine.sql` (tabela `merchant_growth_history` append-only com event_type CHECK). Domínio `src/domains/growth-engine/` com DDD completo: 8 enums (GrowthCategory×10, GrowthStrategyType×10, GrowthPriority×4, GrowthEffort×3, GrowthStatus×5, GrowthEventType×4, OpportunityCategory×8, PlanTier×3), 10 interfaces de tipos (DraftRecommendation, GrowthRecommendation, TodaysPlan, OpportunityCenter, GrowthHistoryEntry, GrowthTimeline, GrowthDashboard, GrowthEvidence, GrowthPriorityBreakdown), GrowthContext (sem importar merchant-decision). 10 Strategies puras implementando GrowthStrategy interface: CatalogGrowthStrategy, TrustGrowthStrategy, VisibilityGrowthStrategy, PricingOpportunityStrategy, DemandOpportunityStrategy, FreshnessStrategy, MerchantProfileStrategy, TrafficOpportunityStrategy, ConversationStrategy, ReviewGrowthStrategy — cada uma com evidências, fontes e impacto esperado. StrategyRegistry (Map estático, idempotente), bootstrapStrategies (guard `let bootstrapped = false`). PriorityEngine: fórmula transparente auditável (impact 0-40 por GrowthCategory + urgency 0-30 por GrowthPriority + ease 0-20 invertido por GrowthEffort + context 0-10 por merchantScore = total máx 100); `reason` gerado em português. RecommendationEngine: fault-isolated por estratégia, deduplicação por `id`. TodaysPlanService: max 5 itens, apenas PlanTier.Free, total_available inclui todos. OpportunityCenterService: filtra por opportunity_category !== null. GrowthHistoryService + IGrowthHistoryRepository + SupabaseGrowthHistoryRepository. Factory `lib/growth-engine-factory.ts`. 6 APIs: GET /growth (dashboard completo), GET /growth/today (plano do dia), GET /growth/opportunities (centro de oportunidades), GET /growth/recommendations?priority= (todas as recomendações, filtrável), PATCH /growth/recommendations/[id] (record event_type), GET /growth/history?limit= (timeline). 6 widgets em `components/merchant/growth-center/widgets/`: TodaysPlanWidget (plano do dia com breakdown de prioridade), TopOpportunitiesWidget (oportunidades por categoria), HighImpactActionsWidget (client, expansível, aceitar/ignorar com recordEvent), CompletedGrowthWidget, GrowthTimelineWidget (timeline com ícone por evento), RecommendationHistoryWidget (métricas de engajamento). Dashboard: Growth Center como 1ª aba (padrão), ícone TrendingUp, badge emerald, fetch lazy. Brain: 12 novos TrustEventType (total 85): GrowthCenterViewed, GrowthPlanGenerated, GrowthRecommendationViewed, GrowthRecommendationAccepted, GrowthRecommendationIgnored, GrowthRecommendationCompleted, GrowthOpportunityIdentified, GrowthOpportunityCenterViewed, GrowthStrategyEvaluated, GrowthContextBuilt, GrowthTimelineViewed, GrowthScoreComputed. Testes: 33 novos (CatalogGrowthStrategy×5, TrustGrowthStrategy×5, RecommendationEngine×7, PriorityEngine×10, TodaysPlanService×6) — total 141/141. Quality Gate: lint 0, typecheck 0, 141/141 testes, build verde.

> ✅ **Release 1.6 — Epic 4 — Catalog Intelligence — Sprint 1.6.4 (2026-06-30)**: Quarto Epic do Release 1.6 entregue. Inteligência de catálogo por produto: scoring individual (0–100, pesos: image 30 + category 25 + brand 15 + desc 15 + price 15), 3 estados (Ideal/Attention/Critical), diagnóstico específico por produto, histórico diário de saúde. Migration `0020_catalog_intelligence.sql` (tabela `merchant_catalog_snapshots` com UNIQUE por merchant+dia). Domínio `src/domains/catalog-intelligence/` com DDD completo: 3 enums (ProductHealthStatus, ProductDiagnosisType, CatalogTrend), 7 interfaces de tipos, ICatalogSnapshotRepository + SupabaseCatalogSnapshotRepository (upsert idempotente), ProductHealthService (funções puras: scoreOffer, getProductHealthList, getHealthBreakdown), CatalogHistoryService (classe com repo injetado, detecção de tendência por delta). Factory `lib/catalog-intelligence-factory.ts`. 3 APIs: GET /catalog/health (+ fire-and-forget snapshot), GET /catalog/history (últimos N dias, max 90), GET /catalog/products (paginado, filtrável por status). 4 widgets: CatalogHealthScoreWidget (score + barras de progresso), ProductHealthListWidget (client, filtro All/Critical/Attention/Ideal, cards expansíveis com diagnósticos), CatalogEvolutionWidget (sparkline + badge de tendência), CatalogInsightsWidget (insight principal + breakdown). Dashboard: 5ª aba "Catálogo" (ícone Package), badge laranja quando há problemas, fetch lazy. Brain: 6 novos TrustEventType (total 73). Testes: 23 novos (ProductHealthService 15, CatalogHistoryService 8) — total 108/108. Quality Gate: lint 0, typecheck 0, 108/108 testes, build compilado.

> ✅ **Release 1.6 — Epic 3 — Merchant Decision Engine — Sprint 1.6.3 (2026-06-30)**: Terceiro Epic do Release 1.6 entregue. Motor de decisão transparente, declarativo e completamente explicável: toda recomendação tem evidência rastreável, zero algoritmo opaco. Migration `0019_merchant_decision_actions` (tabela de ações com lifecycle completo). Domínio `src/domains/merchant-decision/` com arquitetura DDD: 7 enums, 7 interfaces (DecisionContext, Recommendation, Opportunity, DecisionAction, PriorityScore, DecisionCenterData), Rule interface pura (fn: context → RuleResult | null), RuleRegistry (Map estático), bootstrapRules (idempotente), 11 regras declarativas em 4 arquivos (catalog×3, trust×3, analytics×3, profile×2), IActionRepository + SupabaseActionRepository, 5 serviços (RecommendationEngine, PrioritizationEngine, OpportunityDetector, ActionService, DecisionContextBuilder). Fórmula de priorização: impact+effort+urgency+category_weight, máx 100pts, completamente auditável. 5 APIs: GET /decision-center (unificado), GET /recommendations (on-demand, substitui legado), GET /opportunities, GET /actions?status=, PATCH /actions/[id]. 6 widgets: TodaysPrioritiesWidget, RecommendationsWidget (client), OpportunitiesWidget, CompletedImprovementsWidget, PendingImprovementsWidget (client), GrowthTimelineWidget. Dashboard: 4 tabs (+ Decision Center), badge amber em prioridades pendentes, fetch lazy, handlers de aceitar/ignorar/concluir. Brain: 10 novos TrustEventType (total 67). Testes: 33 novos (total 85/85) — RuleRegistry (7), RecommendationEngine (7), OpportunityDetector (7), PrioritizationEngine (12). Quality Gate: lint 0, typecheck 0, 85/85 testes, build compilado.

> ✅ **Release 1.6 — Epic 2 — Merchant Analytics Platform — Sprint 1.6.2 (2026-06-30)**: Segundo Epic do Release 1.6 entregue. Infraestrutura completa de analytics comportamental: `buyer_sessions` + `buyer_events` (append-only) + `merchant_analytics_daily` (migration 0018, 9 índices, RLS). Novo domínio `src/domains/merchant-analytics/` com arquitetura DDD: 22 event types, 2 repositórios + implementações Supabase, 6 serviços (EventPlatform, Session, EventStream, MerchantAnalytics, Funnel, Observability). Hook `useAnalytics` (anonymous_id persistente, session_id, batch auto-flush 2s, flush imediato em conversões, detectDeviceType). 8 APIs: 4 públicas (`/api/analytics/events` POST+batch, `/session`, `/funnel`, `/health`) + 4 autenticadas por merchant (`/api/merchant/analytics`, `/products`, `/traffic`, `/events`). 6 widgets: ViewsWidget, TrafficWidget, TopProductsWidget, MerchantTrafficWidget, FunnelWidget, SessionWidget. Dashboard: 3 tabs (Command Center + Analytics + Score & Metas), tab Analytics com seletor de janela e fetch lazy. Brain: 18 novos TrustEventType (total 57), 14 factory functions, 14 mapeamentos em event-registry cobrindo todos os 6 Brain assets. `lib/analytics-factory.ts` como factory unificada de serviços. Testes: 28/28 novos (total 52/52) — EventPlatformService (14), MerchantAnalyticsService (8), FunnelService (6). Quality Gate: lint 0, typecheck 0, 52/52 testes, build compilado.

> ✅ **Release 1.6 — Epic 1 — Merchant Command Center — Sprint 1.6.1 (2026-06-30)**: Primeiro Epic do Release 1.6 entregue. Domínio `src/domains/merchant-intelligence/` criado com arquitetura DDD: 4 serviços puros (ExecutiveSummaryService, MerchantHealthService, CatalogIntelligenceService, QuickActionsService), 5 API Routes (`/api/merchant/command-center` + 4 individuais), 6 widgets reutilizáveis (ExecutiveSummaryWidget, MerchantHealthWidget, CatalogIssuesWidget, QuickActionsWidget, TrustWidget, RecentActivityWidget). Dashboard refatorado com 2 tabs: Command Center (padrão) + Score & Metas (legado preservado). Brain: 8 novos TrustEventType + BrainEntityType.CommandCenter + 8 factory functions + 8 mapeamentos no event-registry. Testes: 24/24 passando (MerchantHealthService + QuickActionsService + CatalogIntelligenceService). Jest configurado em jest.config.ts. Quality Gate: lint 0, typecheck 0, 24/24 testes, build compilado.

> ✅ **Release 1.5 — Epic 4 — Cognitive Integration & Hardening — Sprint 1.5.5 (2026-06-29)**: Release Candidate consolidado. Módulo Brain criado em `src/domains/trust/brain/`: CognitiveBrainService (ingestão unificada com schema_version, correlation_id, validação, logging estruturado, persistência), EventQualityValidator (7 regras: campos obrigatórios, schema version, clock skew), ObservabilityService (structured logs por nível + health check builder), KnowledgeGraphService (derivação de relações Buyer↔Merchant↔Signal↔Review a partir de eventos Brain armazenados), SearchReadinessService (8 boost factors sem algoritmo, readiness_score 0-100 para future ranking). Novos enums: BrainEntityType (9 tipos), GraphRelationType (10 relações), CognitiveBrainActorRole (4 roles). API: GET /api/trust/brain/health (verifica 5 tabelas em paralelo). Teste de integração: TrustFlow.integration.test.ts cobre pipeline completo Verification→Signal→Passport→Review→Brain→KnowledgeGraph (7 asserções em 6 etapas). Hardening: comentário residual removido de MerchantTrustSection. Lint: 0. TypeScript: 0. Build: 100% — /api/trust/brain/health como nova rota.
>
> ✅ **Release 1.5 — Epic 3 — Merchant Identity — Sprint 1.5.4 (2026-06-29)**: Merchant Identity System implementado. Merchant Passport consolida toda a camada de Trust (Epics 1+2) em uma estrutura única, extensível, orientada a fatos. Novos enums: PassportSection (5 seções), MerchantChannelType (4 tipos). TrustEventType: 24→31 eventos Brain. Novos tipos: MerchantBasicData, MerchantChannel, MerchantInsights, PassportSearchMetadata, MerchantPassport. Serviço: MerchantPassportService (getPassport + getInsights) — Promise.all de 7 repos, insights computados (platformAgeInDays, channelBuilding, searchMetadata para future ranking). Eventos Brain: 7 novos (passport_viewed, fact_expanded, timeline_interaction, review_interaction, profile_shared, contact_clicked, location_viewed) — registry expandido para 31 mapeamentos. APIs: GET /passport, GET /insights. Página /lojistas/[merchantId] com 5 abas (Visão Geral, Trust, Timeline, Avaliações, Informações), ProfileTabNav SSR via Link + searchParams, MerchantHeader com canais verificados. Componentes (11): TrustExplainabilityCard (why/who/when/evidence), MerchantHeader, MerchantFacts, MerchantMetrics, MerchantHighlights, MerchantIdentityCard, MerchantTrustSection, MerchantHistorySection, MerchantOverview, MerchantSidebar, ProfileTabNav. Test: MerchantPassportService.test.ts (6 asserções). Lint: 0. TypeScript: 0. Build: OK.
>
> ✅ **Release 1.5 — Epic 2 — Trust Experience — Sprint 1.5.3 (2026-06-29)**: Sistema completo de Trust Experience implementado. 16 partes do Epic 2 entregues. Novas tabelas: `trust_signals`, `signal_provenance`, `merchant_reviews`, `review_reports`, `review_history` (INSERT-ONLY), `merchant_timeline` — migration `0016_trust_experience.sql` com RLS completo. Novos enums: TrustSignalType (15 valores), TrustSignalStatus, TrustSignalCategory, SignalTrustLevel, ReviewStatus, ReviewAction, ReviewReportReason, ReviewReportStatus, TimelineEventType (15 valores), TimelineEventCategory, TimelineVisibility. TrustEventType expandido de 14 para 24 eventos Brain. Domínio: 6 novas entidades (TrustSignalEntity, SignalProvenance, MerchantReview, ReviewReport, ReviewAudit, MerchantTimelineEvent), 6 novas interfaces de repositório, 6 implementações Supabase. 5 novos serviços: TrustSignalService (com VERIFICATION_TO_SIGNAL mapping automático), ReviewService (CRUD completo com audit), ReviewModerationService (fila admin), MerchantTimelineService, MerchantProfileService. Eventos: +10 factory functions em trust.events.ts; event-registry.ts expandido de 14 para 24 mapeamentos Brain (agora cobre todos os 24 TrustEventType). APIs: 13 novos endpoints (profile, signals, timeline, reviews CRUD, review report, reply, history, admin moderation, admin reports). Páginas: `/lojistas/[merchantId]` (perfil público de trust com timeline, reviews, sinais, reputação), `/admin/trust/reviews` (fila de moderação), `/admin/trust/reviews/[id]` (detail com ações). Componentes reutilizáveis: TrustSignalCard, TrustBadgeGrid, TrustPanel, TrustSummary, ReviewCard, ReviewList, ReviewComposer (client), MerchantTimeline + TimelineEmptyState + TimelineSkeleton, ReputationOverview, VerificationWidget, EvidencePreview, TimelineFilters (client). AdminSidebar: Trust grupo agora tem 3 filhos (Dashboard, Verificações, Reviews). Teste: ReviewService.test.ts (8 asserções). Brain: 24 eventos com mapeamento completo para 6 ativos (HistoricalData, MerchantTrust, KnowledgeGraph, BuyerBehavioralKnowledge, SearchIntelligence, RecommendationKnowledge). Lint: 0 erros. TypeScript: 0 erros. Build: sucesso — `/lojistas/[merchantId]` como nova rota dinâmica.
>
> ✅ **Release 1.5 — Sprint 1.5.2 — Merchant Verification (2026-06-29)**: Sistema de Verificação de Merchants implementado. Modelo multi-tipo, baseado em evidências, auditável, extensível. 13 etapas concluídas. Arquivos novos/modificados: enums expandidos (VerificationType +8, VerificationStatus +Revoked, +VerificationAction, +EvidenceType, +VerificationCategory, +TrustEventType +4 = 15 event types), trust.types.ts (+VerificationTypeCatalogRecord, +VerificationEvidenceRecord, +VerificationAuditRecord), 4 novas entidades de domínio (VerificationEvidence, VerificationHistory, VerificationTypeCatalog, VerificationResult), 3 novas interfaces de repositório (IVerificationEvidenceRepository, IVerificationHistoryRepository, IVerificationTypeCatalogRepository), 3 novas implementações Supabase, 3 novos serviços (VerificationAuditService, VerificationHistoryService, VerificationEvidenceService), VerificationService expandido (+revokeVerification, +getVerificationById, +getVerificationResult, +VerificationAuditService injeção opcional), 5 novos endpoints de API (GET/PATCH /merchant/[id]/verification/[vid], GET /merchant/[id]/verification/[vid]/history, GET/POST /merchant/[id]/verification/[vid]/evidence, GET /merchant/[id]/verification/history, GET /verification-types), 2 novas páginas admin (/admin/trust/verifications, /admin/trust/verifications/[id]), VerificationActionsClient (approve/reject/revoke com motivo), 6 componentes de UI (VerificationBadge, VerificationTimeline, VerificationStatusDisplay, VerificationCard, EvidenceViewer, HistoryTable), AdminSidebar atualizado (Trust → grupo com Dashboard + Verificações), migration 0015 (ALTER verification_type CHECK +8 tipos, ALTER status CHECK +revoked, CREATE verification_types catalog com 15 seeds, CREATE verification_evidence, CREATE verification_history audit log + RLS completo), event-registry.ts expandido (+4 event types com Brain mapping), trust.events.ts +7 factory functions. Testes: VerificationService.test.ts, VerificationAuditService.test.ts, TrustService.test.ts, trust.validators.test.ts, types.test.ts — todos convertidos para assertion-based (zero dependência de framework). Lint: 0 erros. TypeScript: 0 erros. Build: sucesso. Aguardando autorização do CTO para Sprint 1.5.3 — Trust Signals.
>
> ✅ **Release 1.5 — Sprint 1.5.1 — Trust Infrastructure (2026-06-29)**: Domínio `src/domains/trust/` criado com arquitetura DDD completa. 39 arquivos. Estrutura: `types/` (enums + interfaces), `domain/` (5 entidades: MerchantTrust, TrustEvent, Verification, Badge, TrustHistory), `repositories/` (5 interfaces), `infrastructure/` (5 implementações Supabase), `services/` (5 serviços: TrustService, VerificationService, BadgeService, TrustHistoryService, EventService), `events/` (trust.events.ts + event-registry.ts com Brain impact declarado para 10 event types), `validators/` (validadores de transição, score, badge, verificação, eventos), `mappers/`, `utils/`, `tests/` (3 suítes: tipos, serviços, validadores). Migration `0014_trust_foundation.sql` com 5 tabelas (merchant_trust, merchant_trust_events, merchant_verifications, merchant_badges, trust_history), índices e RLS completos. 5 endpoints: GET/POST /api/trust/merchant/[id], GET /api/trust/merchant/[id]/history, GET/POST /api/trust/merchant/[id]/badges, GET/POST /api/trust/merchant/[id]/verification, GET/POST /api/trust/events. Página admin `/admin/trust` com estatísticas e tabela de registros. AdminSidebar atualizado com entrada "Trust". Brain: 4 ativos declarados (HistoricalData, MerchantTrust, KnowledgeGraph, BuyerBehavioralKnowledge) com mapeamento por event type. Build: 65 rotas → +8 novas rotas (trust domain). Lint: 0 erros. TypeScript: 0 erros. Aguardando Sprint 1.5.2.
>
> ✅ **Foundation 0.9 — FOUNDATION VALIDATION (2026-06-27)**: Validação completa da Foundation Empresarial. 9 etapas executadas: leitura completa, matriz de responsabilidades, consistência conceitual (ParaguAI Brain, Merchant OS, Merchant Score, Flywheel, Moat, Compounding, IA Transversal, Assimetria de Informação), referências cruzadas, duplicidades, lacunas, teste de governança (5 cenários), avaliação de maturidade (9.0/10), certificação. Única inconsistência real encontrada: AI_CONSTITUTION Seção XIX desatualizada (não incluía os 7 documentos criados após v1.1). Corrigida: Seção XIX atualizada para hierarquia completa de 12 documentos; AI_CONSTITUTION promovida para v1.2. Foundation Empresarial declarada **CERTIFICADA v1.0**.
>
> ✅ **Foundation 0.8 — RELEASE_STRATEGY.md (2026-06-27)**: `docs/foundation/RELEASE_STRATEGY.md` criado — o ciclo permanente de evolução da plataforma. Fecha a Foundation. 15 seções: Filosofia de Releases (evolução mensurável, não conjunto de tarefas; o que fica é mais importante do que o que entra), O Ciclo Permanente (11 estágios: Missão → Observação → Formulação → Decision Filter → ADR → Planejamento → Implementação → Validação → Release → Observação → Aprendizado → Missão), O Que É uma Release (coeso, evolução identificável, 5 propriedades), O Que NÃO É (acúmulo, sem valor, cosméticas, experimentos sem hipótese, sem critério, reversão sem aprendizado), 10 Tipos de Release (Foundation/Architecture/Platform/Feature/Quality/Infrastructure/Security/Performance/Data/AI) com objetivos e Quality Gates específicos por tipo, Critérios para Criar Release (impacto em múltiplos módulos / decisão arquitetural / novo ativo / mudança de comportamento / mudança de dados), Planejamento (problema, objetivo, escopo, critério de sucesso, dependências, riscos — sem prazo de calendário), Compounding Releases (motor/dado/contrato/IA/conhecimento; pergunta de compounding obrigatória), Definition of Ready (8 condições), Definition of Done (10 condições), Quality Gates (5 universais + gates adicionais por tipo), Versionamento (Foundation F.x / Major x.0 / Minor x.y / Patch x.y.z; CHANGELOG como significado real), Comunicação (CHANGELOG/PROJECT_STATUS/ADRs/NEXT_STEPS), Aprendizado Contínuo (4 dimensões), O Compromisso (7 compromissos permanentes). Tabela final com os 8 documentos e o ciclo fechado.
>
> ✅ **Foundation 0.7 — DECISION_FILTER.md (2026-06-27)**: `docs/foundation/DECISION_FILTER.md` criado — processo permanente de tomada de decisão. 12 seções: O Papel do Filter (alinhamento, valor, coerência, experiência), Pergunta Fundamental (reduz assimetria de informação?), Pipeline de Decisão (10 estágios sequenciais: problema real → valor gerado → missão → North Star → Business Model → Vision 2035 → Engineering → Product → custo/reversibilidade → decisão), 12 Filtros Permanentes (4 críticos bloqueadores), Critérios de Priorização (valor para missão, impacto sistêmico, compounding, esforço), Tipos de Decisão (Nível 1 operacional / Nível 2 produto / Nível 3 estratégico), Critérios de "Não" (9 situações), Critérios de "Sim" (8 situações), Conflitos Entre Critérios (velocidade vs. qualidade, curto vs. longo prazo, receita vs. confiança, automação vs. controle, completude vs. entrega, Foundation vs. circunstância), Checklist Obrigatório (reutilizável para Releases/ADRs/funcionalidades), 10 Anti-Patterns de Decisão (feature por vaidade, arquitetura por moda, escalamento prematuro, automação desnecessária, duplicação, tecnologia acima do problema, consenso por exaustão, urgência fabricada, exceção que vira regra, decisão pela ausência de questionamento), O Compromisso. Foundation de 7 documentos completa.
>
> ✅ **Foundation 0.6 — PRODUCT_PRINCIPLES.md (2026-06-27)**: `docs/foundation/PRODUCT_PRINCIPLES.md` criado — filosofia permanente de produto. 18 seções + 12 Princípios Permanentes. Filosofia de Produto (produto melhora decisões, não gera cliques), Quem Servimos (4 públicos com necessidades distintas), Simplicidade Radical (complexidade pertence ao sistema), Transparência (origem/atualização/raciocínio/incerteza), IA como Assistente (amplifica julgamento, não substitui; transparência algorítmica), Automação Inteligente (dados vs. contexto humano), Produto Orientado a Dados (cada funcionalidade produz dados), Confiança como Produto (construída, não declarada; custo de quebra assimétrico), Experiência Integrada (teste de integração, padrões compartilhados), Produto Modular (capacidades que ampliam o ecossistema), Crescimento Invisível (progressividade, padrões inteligentes), Feedback Contínuo (comportamento > pesquisa), Acessibilidade (diversidade tecnológica/experiência/linguística/física), Neutralidade (rankings orgânicos, publicidade identificada, recomendações por relevância), Efeito "Uau" (funcionalidades que mudam comportamento), Produto como Ecossistema, Produto para Décadas (implementação evolui; dados históricos são permanentes). Foundation de 6 documentos completa.
>
> ✅ **Foundation 0.5 — ENGINEERING_PRINCIPLES.md (2026-06-27)**: `docs/foundation/ENGINEERING_PRINCIPLES.md` criado — filosofia técnica permanente. 15 seções: Filosofia (tecnologia como meio), Arquitetura Evolutiva (composição, contratos > implementações), Simplicidade como Estratégia, Sistemas Orientados a Ativos, Dados como Contrato (origem/propriedade/imutabilidade), APIs e Fronteiras, Escalabilidade (O(1) no design, idempotência), Observabilidade (logs + métricas + rastreabilidade), Automação (dry-run, falha ruidosa), IA como capacidade transversal, Evolução Contínua, Qualidade como design constraint, Segurança (menor privilégio, defesa em profundidade), Resiliência (falha previsível, degradação graciosa), 12 Princípios Permanentes. Foundation de 5 documentos completa.
>
> ✅ **Foundation 0.4 — VISION_2035.md (2026-06-27)**: `docs/foundation/VISION_2035.md` criado — horizonte estratégico do ParaguAI. 15 seções: Introdução, Ambição (infraestrutura de inteligência da Tríplice Fronteira), Transformação (6 estágios evolutivos), Ecossistema (9 pilares), ParaguAI Brain (visão de convergência), Experiência do Usuário/Lojista 2035, Turismo Inteligente integrado, Plataforma Aberta, Inteligência Regional (ética de dados), Expansão, Cultura Permanente (5 princípios), O Legado, Indicadores qualitativos (6), Manifesto Final. Núcleo estratégico de 4 documentos completo.
>
> ✅ **Foundation 0.3 — BUSINESS_MODEL.md (2026-06-27)**: `docs/foundation/BUSINESS_MODEL.md` criado — modelo econômico permanente do ParaguAI. 18 seções: Tese, Assimetria de Informação, Proposta de Valor por segmento (5 grupos), Flywheel Econômico, 6 Network Effects, Monetização (7 pilares), Crescimento orgânico, Moat (5 camadas), Ativos Estratégicos (8), papel da IA/Dados/Turismo/Merchant OS/API, Indispensabilidade, Visão Econômica, 6 Riscos Estratégicos, 7 Princípios Permanentes. `CLAUDE.md` e `MASTER_ROADMAP.md` atualizados com BUSINESS_MODEL como terceiro documento obrigatório. Núcleo estratégico completo: AI_CONSTITUTION + NORTH_STAR + BUSINESS_MODEL.
>
> ✅ **Foundation 0.2.1 — NORTH_STAR.md Maturity Review (2026-06-27)**: `docs/foundation/NORTH_STAR.md` revisado v1.0 → v1.1. 4 novos capítulos: Anti Goals (9 itens), Tipos de Decisão (Tipo 1/Tipo 2), Compounding Decisions, O Compromisso (fechamento permanente). Seção 8 fortalecida com princípio de infraestrutura reutilizável. Total: 14 seções. Documento declarado permanente.
>
> ✅ **Foundation 0.2 — NORTH_STAR.md (2026-06-27)**: `docs/foundation/NORTH_STAR.md` criado — bússola operacional do ParaguAI. 10 seções: A Métrica Norte, A Pergunta Obrigatória, 10 Filtros Permanentes, Hierarquia de Prioridades, Framework de Priorização (score/100), O que nunca priorizar, Pensamento de longo prazo, Como avaliamos sucesso, Checklist Final. `CLAUDE.md` atualizado com NORTH_STAR como segundo documento obrigatório.
>
> ✅ **Foundation 0.1.1 — Revisão Estratégica da AI_CONSTITUTION.md (2026-06-27)**: `docs/foundation/AI_CONSTITUTION.md` revisado de v1.0 para v1.1. 3 novos capítulos: VII. Ativos, X. Efeito de Rede (Flywheel), XIV. Moat. Identidade reformulada: "Inteligência Operacional da Tríplice Fronteira". Organismo Vivo na Filosofia. ParaguAI Brain expandido. 14 Regras Permanentes (era 12). Missão: assimetria de informação. Total: 19 seções vs. 16 na v1.0.
>
> ✅ **Foundation 0.1 — AI_CONSTITUTION.md (2026-06-27)**: Constituição permanente do projeto criada em `docs/foundation/AI_CONSTITUTION.md`. `CLAUDE.md` atualizado com prioridade de leitura. `MASTER_ROADMAP.md` expandido com seção Foundation Documents e fases estratégicas. 16 seções: Identidade, Missão, Visão 2030, North Star, Filosofia, Dados, Automação, IA, Engenharia, Produto, Negócio, Autonomia, Regras Permanentes (12 regras), Processo de Desenvolvimento, Critério de Aceitação, Hierarquia de Documentos.
>
> ✅ **Release 1.4 — Merchant Growth Platform (2026-06-27)**: 10 módulos. Novos: `/lojas` ranking, `/lojas/[slug]` página pública premium, `MerchantProgressCard`, `GoalsPanel` expandido com 10 missões, `stores-public.service.ts` (service role). SEO: `/lojas` e `/lojas/[slug]` no sitemap. Navbar: "Lojas" adicionado. ADR-036 a ADR-039. Build: 67 rotas, lint 0, tsc 0.
>
> ✅ **Release 1.3.1 — ParaguAI Experience Integration (2026-06-27)**: `HeroCTAs`, `ForLojistasSection`, `/para-lojistas`, "Para Lojistas" no Navbar/Footer. Build: 64 rotas.
>
> ✅ **Release 1.3 — Dashboard Consultivo (2026-06-27)**: `NextStepCard`, `GoalsPanel`, 6 níveis de Merchant, `computeNextStep`/`computeGoals`/`getMerchantLevel`.
>
> ✅ **Release 1.2 — Merchant OS (2026-06-26)**: portal self-service completo, 10 endpoints, 11 rotas, migration 0012.

> ✅ **Release 0.9 — Acquisition Engine (2026-06-26)**: pipeline universal de aquisição de dados implementado. Engines: Validation, Normalization, Deduplication, Canonical, Media, CatalogWriter. Parsers: JSON e CSV (RFC 4180). Conectores de referência: JsonFileConnector, CsvFileConnector. 33/33 asserções do validate-pipeline OK. Dry-run de import JSON e CSV: 4/4 itens por run, 0 erros. Lint: 0. TypeScript: 0. Build: sucesso. A partir desta Release, adicionar uma nova loja exige apenas implementar um novo conector.
>
> ✅ **Migration 0008 aplicada (2026-06-26)**: 4 UNIQUE constraints em slug (stores, products, brands, categories) + 6 índices de performance confirmados no banco.

> ✅ **ADR-019 ENCERRADO (2026-06-25)**: migration `0007_proposed_public_read_policies.sql` aplicada manualmente no Supabase SQL Editor pelo CTO. Validação com `database/seed/validate_adr019.js` confirma: **22 OK | 0 FAIL** usando **exclusivamente** `NEXT_PUBLIC_SUPABASE_ANON_KEY`. As 6 tabelas públicas (`brands`, `categories`, `products`, `offers`, `price_history`, `stores`) retornam dados reais. Escrita continua bloqueada para `anon`/`authenticated`. O catálogo inteiro agora é visível para usuários reais.
>
> ✅ **Bugs críticos corrigidos (Sprint 3.5)**: os bugs confirmados na Sprint 3.4.1 (`offer.price`/`stock`/`installments`/`url`, `store.banner_url`/`verified` divergindo do schema real) foram corrigidos antes de construir o catálogo de produtos sobre eles — ver `docs/operations/DECISIONS.md` ADR-009. `types/offer.ts`/`types/store.ts` agora usam os nomes reais (`price_usd`/`price_brl`, `in_stock`, `product_url`, `cover_image`, `is_verified`, mais os 13 campos de contato/horário que já existiam no banco).
>
> ⚠️ **Achado de dados (Sprint 3.8) — corrigido pelo ADR-019**: `npm run db:seed:execute` rodou com sucesso contra o Supabase real (`stores: 5`, `brands: 5`, `categories: 5`, `products: 6`, `offers: 9`) e ADR-007 (dados ausentes) está resolvido **a nível de banco**. A afirmação original de que isso tornava os domínios "navegáveis em produção" estava incompleta — não havia sido verificada com a chave anônima (ver alerta crítico acima).
>
> ✅ **Price Engine v1 validado (Sprint 3.9, adendo, ADR-018)**: `updateOfferPrice`/`getOfferPriceMetrics` testados fim a fim contra `price_history` real (criada manualmente pelo CTO) — histórico, no-op, métricas (`lowest`/`highest`/variação %) todos corretos após um bug de cálculo ser encontrado e corrigido durante a validação. Classificação: **"Backend Production Ready"** — correto e testado, mas sem nenhum caminho de chamada real ainda, e a leitura pública (`getOfferPriceMetrics` via app) está sujeita ao mesmo bloqueio de RLS do ADR-019.

## Sprint 4.1 + ADR-019 Encerramento — Public Release Readiness (Release 0.6)

**Home dinâmica** com dados reais do Supabase (stores, brands, categories, catalog) — `app/page.tsx` convertida de dados hardcoded para `async` server component com `force-dynamic`. **Double-fetch eliminado** em `/product/[slug]` e `/store/[slug]`: pages convertidas de `"use client"` + hook para server components com `React.cache()` compartilhado via `app/...[slug]/_cache.ts` (ADR-021). **Botão "Comparar preços"** adicionado ao produto, linkando para `/compare/[slug]`. `constants/categories.ts` esvaziado (mock substituído por `getCategories()` real). `ProductHighlight.priceUSD`/`storeName` tornados opcionais para compatibilidade com `ProductCatalogItem`.

**ADR-019 ENCERRADO**: `0007_proposed_public_read_policies.sql` aplicada. Validação completa com `validate_adr019.js` — 22 asserções, 0 falhas, exclusivamente com a chave anônima. Todos os domínios públicos desbloqueados.

Build: 8 rotas (`/` agora `ƒ Dynamic`). Lint: 0 erros. TypeScript: 0 erros. db:validate: 0 problemas.

---

## Visão geral

**ParaguAI** é um marketplace de comparação de preços (UI em português) para lojas do Paraguai/região de fronteira. Usuários pesquisam produtos/lojas/marcas; o produto final prevê busca assistida por IA, histórico de preços e recomendações. Mercado inicial: Ciudad del Este.

## Objetivo do produto

Tornar-se a maior plataforma inteligente de compras do Paraguai, ajudando o usuário a: pesquisar produtos, comparar preços entre lojas, descobrir lojas confiáveis e receber recomendações de compra via IA — antes de atravessar a fronteira.

## Stack utilizada

- **Next.js 16.2.9** (App Router, Turbopack) + **React 19.2.4** + **TypeScript** (strict)
- **Tailwind CSS v4** (`@theme inline` em `app/globals.css`, sem `tailwind.config`)
- **Supabase** (`@supabase/supabase-js`) como backend/DB (PostgreSQL)
- **lucide-react** para ícones
- Hospedagem/deploy: **Vercel**
- Sem suíte de testes configurada. Sem CI (não há `.github/workflows` nem `vercel.json` no repositório).

## Arquitetura atual

Fluxo em camadas, conforme `CLAUDE.md` e `docs/architecture/ARCHITECTURE.md`:

```
types/*.ts → services/*.service.ts → hooks/use*.ts → components/* → app/*
```

Ver `docs/architecture/ARCHITECTURE.md` para o mapeamento completo, incluindo o novo fluxo do catálogo de produtos.

## Funcionalidades implementadas

- **Home (`/`)** — **Sprint 4.1**: agora busca dados reais do Supabase em paralelo (`getStores`, `getBrands`, `getCategories`, `getProductsCatalog`) com `force-dynamic`. Até `0007` ser aplicado, apenas a seção de lojas exibe dados reais (única tabela com leitura pública).
- **Página de Produto (`/product/[slug]`)** — integração real ponta-a-ponta. **Sprint 4.1**: convertida de `"use client"` + `useProduct` para Server Component com `React.cache()` via `_cache.ts` — eliminando o double-fetch. Botão "Comparar preços" adicionado, linkando para `/compare/[slug]`. `generateMetadata`, JSON-LD (Product), estados `loading`/`error`/`not-found`. Breadcrumb via `components/ui/Breadcrumb.tsx`.
- **Favoritos** — `useFavorites` funcional via `localStorage`, sem dependência de Supabase/autenticação.
- **Navbar/Footer** — completos, estáticos. `/products` deixou de ser link morto (Sprint 3.5).
- **Sistema de motion/animação** — `styles/animations.ts` + keyframes, usado na maioria dos componentes, respeitando `prefers-reduced-motion`.
- **Busca (`/search`)** — fluxo completo ponta-a-ponta, com filtros/paginação/autocomplete ainda pendentes (ver `TECH_DEBT.md`).
- **Página de Loja (`/store/[slug]`)** — integração real ponta-a-ponta, espelhando o Domínio de Produto. **Sprint 3.5**: `StoreDetails` ganhou a seção de Contato/Horário (telefone, WhatsApp, e-mail, site, endereço, horário), antes bloqueada por uma premissa de schema incorreta (ADR-006/ADR-009); avaliações continuam `EmptyState` honesto.
- **Catálogo de Produtos (`/products`)** — **novo nesta sprint (Release 0.2, parte 2)**: listagem com filtros (categoria, marca, loja, faixa de preço, disponibilidade, busca textual) e ordenação (mais recentes / menor preço / maior preço, todos sincronizados com a URL; "mais vendidos"/"melhor avaliação" como estrutura preparada), paginação SSR via `<Link>`, `generateMetadata` (canonical/OG/Twitter/robots por combinação de filtro), JSON-LD `CollectionPage`+`ItemList`+`BreadcrumbList`, `<Suspense>` em torno da listagem (filtros renderizam fora do Suspense). Ver `docs/product/FEATURES.md` para o detalhe completo.

## Funcionalidades parcialmente implementadas

Nenhuma nesta auditoria — `ProductGrid.tsx` (única pendência apontada na auditoria anterior) foi implementado nesta sprint.

## Funcionalidades não iniciadas

- Listagem `/stores`, `/compare`, `/favorites`, `/price-history`, `/about`, `/contact`, `/privacy`, `/terms` — ainda linkadas no `Navbar`/`Footer`, nenhuma existe (404 em produção).
- Autenticação de usuário (`types/user.ts` vazio, sem Supabase Auth configurado no código da aplicação — embora a tabela `profiles` sugira que a infraestrutura pode já existir no painel, ver `DOMAIN_MODEL.md`).
- Reviews (`types/review.ts` vazio; tabela `reviews` não existe no Supabase).
- IA/Assistente de compras (`services/ai.service.ts` vazio; `ai/` é só placeholders `.gitkeep`).
- Histórico de preços, crawler, painel admin, marketplace multi-vendedor — nada começado.
- Design system formal (`styles/theme.ts`, `typography.ts`, `spacing.ts`, `radius.ts`, `shadows.ts` — todos vazios; cores/espaçamento hoje são hardcoded inline via Tailwind arbitrary values).

## Páginas existentes

| Rota | Tipo | Status |
|---|---|---|
| `/` | Server Component (`force-dynamic`) | Dados reais, ForLojistasSection, HeroCTAs auth-aware |
| `/search` | Server Component (com `<Suspense>`) | Completa, integrada ao Supabase |
| `/products` | Server Component (com `<Suspense>`) | Completa, integrada ao Supabase |
| `/product/[slug]` | Server Component + Server layout | Double-fetch eliminado; botão "Comparar preços" |
| `/store/[slug]` | Server Component + Server layout | Double-fetch eliminado |
| `/compare/[slug]` | Server Component | Integrada ao Supabase |
| `/lojas` | Server Component (`force-dynamic`) | **Release 1.4**: ranking de lojas com Merchant Score, SEO completo |
| `/lojas/[slug]` | Server Component (`force-dynamic`) | **Release 1.4**: página pública premium, JSON-LD LocalBusiness |
| `/para-lojistas` | Server Component (estático) | Landing institucional, planos, FAQ |
| `/merchant/dashboard` | Client Component | Stats + Score + GoalsPanel + MerchantProgressCard + NextStepCard |
| `/merchant/register` | Client Component | Cadastro PKCE flow |
| `/merchant/login` | Client Component | Login com `?confirmed=true` banner |
| `/api/merchant/dashboard/stats` | Route Handler | Stats + score + goals + profileCompletion |

## Componentes existentes

- `home/`: 10 componentes implementados; `Offers` agora renderiza `ProductCard` (antes `ProductHighlightCard`, removido).
- `layout/`: Navbar, Footer — implementados.
- `product/`: `ProductCard` (unificado, ADR-010), `ProductGrid`/`ProductGridSkeleton`/`ProductFilters` (novos, Sprint 3.5), `ProductGallery`, `ProductHeader`, `ProductSpecifications`, `ProductOffers`, `RelatedProducts`, `FavoriteButton`, `ShareButton` — todos implementados. `ProductHighlightCard`/`ProductBreadcrumb` removidos.
- `store/`: StoreCard, StoreDetails, StoreOffers, StoreGrid — todos implementados.
- `search/`: SearchResults, SearchResultsSkeleton — implementados.
- `ui/`: 16 implementados (12 anteriores + `EmptyState` + `Breadcrumb`/`Pagination`/`Input`/`Select`, novos/preenchidos na Sprint 3.5). `Card`, `Loading`, `SearchInput` — vazios (3).

Ver `docs/architecture/COMPONENT_INDEX.md` para o detalhe item a item.

## Hooks existentes

- `useProduct`, `useFavorites`, `useSearch`, `useStore` — implementados.
- `useProductFilters` (Sprint 3.5) — implementado, sincroniza os filtros do catálogo com a URL.
- `useOffers` — arquivo vazio (placeholder, sem consumidor planejado nesta sprint).

## Services existentes

- `product.service.ts` — implementado (`getProducts`, `getProductBySlug`, `getRelatedProducts`, `searchProducts`, `getProductsCatalog` novo na Sprint 3.5).
- `offer.service.ts` — implementado (`getOffers`, `getOffersByProduct`, `getOffersByStore`; ordenação corrigida para `price_usd` na Sprint 3.5). **Sprint 3.9**: `updateOfferPrice`/`getOfferPriceMetrics` novos (Price Engine v1, ADR-017) — code-complete e testados (degradação graciosa confirmada), sem consumidor ainda; dependem de `price_history`, tabela proposta (`0006`) mas não aplicada no Supabase real.
- `store.service.ts` — implementado (`getStores`, `getStore`, `getStoreBySlug`, `getRelatedStores`).
- `search.service.ts` — implementado, `searchEverything`.
- `category.service.ts`, `brand.service.ts` — **implementados na Sprint 3.5** (antes vazios): `getCategories`/`getCategoryBySlug`, `getBrands`/`getBrandBySlug`.
- `ai.service.ts` — vazio.

## Tipos existentes

`Product`/`ProductWithRelations`/`ProductCatalogItem` (novo, Sprint 3.5)/`ProductHighlight`, `Offer`/`OfferWithStore`/`OfferWithProduct` (corrigidos para o schema real na Sprint 3.5, ADR-009), `Store` (corrigido, idem), `Brand`, `Category`, `Favorite`, `Search` — implementados. **`PriceHistoryEntry`/`OfferPriceMetrics`/`PriceUpdateResult`** (`types/priceHistory.ts`, novo, Sprint 3.9) — únicos tipos do projeto que descrevem um schema proposto, não confirmado no banco real (ver ADR-017). `User`, `Review` — vazios.

## Providers existentes

**Nenhum.** Sem `providers/`, sem React Context global, sem theme/auth provider. Estado global (favoritos) é resolvido via módulo singleton + `useSyncExternalStore`.

## Integração com Supabase

Cliente único em `lib/supabase.ts`, criado a partir de `lib/env.ts` (única fonte de `process.env`, ADR-001).

Esquema do banco documentado em `docs/database/DATABASE.md`/`ERD.md` (não código, apenas descrição); `database/migrations` tem 3 propostas não aplicadas (`0001` superada, `0002` integridade de `stores`, `0003` view de agregação de preço para o catálogo — Sprint 3.5). O schema real continua existindo apenas no painel do Supabase.

Auditoria direta confirmou (Sprint 3.4.1, corrigido no código na Sprint 3.5): `stores` tem 24 colunas reais, `offers` tem 16 — ambas agora totalmente modeladas em `types/`. **Atualizado na Sprint 3.8**: `stores` tem 5 linhas reais, todas com `slug`/`active` preenchidos (ADR-007 resolvido); `brands: 5`, `categories: 5`, `products: 6`, `offers: 9` — dados de exemplo carregados via `database/seed/`, auditados sem FK órfã, slug duplicado ou inconsistência (ver relatório da Sprint 3.8).

## Integração com Vercel / Deploy

Projeto linkado a um projeto Vercel. Variáveis de ambiente do Supabase precisam estar configuradas no painel do projeto Vercel.

## CI atual

**Não existe.** Nenhum workflow de GitHub Actions, nenhum hook de pre-push automatizado além do que o Vercel roda no próprio build de deploy.

## Status do build

✅ `npm run build` — ver relatório da Sprint 3.5 para o resultado mais recente (rotas esperadas: `/`, `/_not-found`, `/product/[slug]`, `/search`, `/store/[slug]`, `/products` — nova nesta sprint).

## Status do lint

✅ `npm run lint` — ver relatório da Sprint 3.5 para o resultado mais recente.

## Status do TypeScript

✅ `npx tsc --noEmit` — ver relatório da Sprint 3.5 para o resultado mais recente.

## Sprint 3.2 — Consolidação (sem novas features de negócio)

Unificação do acesso a `process.env` em `lib/env.ts` (ADR-001), correção do `.gitignore` (ADR-002), limpeza de scripts (ADR-003/004), criação de 6 documentos permanentes de engenharia.

## Sprint 3.3 — Domínio de Busca (Release 0.4, parte 1)

Liga a busca de ponta a ponta: `app/search/page.tsx`, `hooks/useSearch.ts`, `services/search.service.ts`, `SearchResults` com estados reais, `EmptyState` (novo, reaproveitável), JSON-LD `WebSite`/`SearchAction` no root layout.

## Sprint 3.4 — Domínio de Loja (Release 0.3)

Fecha o terceiro domínio central da Home, espelhando a arquitetura do Domínio de Produto: `getStoreBySlug`/`getRelatedStores`, `getOffersByStore`, `hooks/useStore.ts`, `StoreDetails`/`StoreGrid`/`StoreOffers`, `app/store/[slug]/` completo, `storePath`/`storeUrl`.

## Sprint 3.4.1 — Consolidação da Camada de Dados (auditoria, sem novas telas)

Auditoria direta do Supabase real (sem alterar código): confirmou que `types/offer.ts`/`types/store.ts` divergem do schema real (ADR-008), que contato/horário de loja já existiam no banco (corrigindo ADR-006), e que 4 FKs usadas pelos services são reais. Nenhuma correção de código aplicada — ficou para aprovação.

## Sprint 3.5 — Catálogo Premium de Produtos (Release 0.2, parte 2)

Duas frentes, nesta ordem (decisão tomada com o CTO antes de iniciar a implementação, ver pergunta de decisão registrada na sessão):

1. **Correção do modelo de dados** (ADR-009): `types/offer.ts`/`types/store.ts` corrigidos para os nomes reais do schema (ver aviso no topo deste documento); `utils/currency.ts` perdeu a conversão por taxa fixa (não tem mais consumidor); `StoreDetails.tsx` ganhou a seção de Contato/Horário.
2. **Catálogo de produtos** (`/products`): `services/category.service.ts`/`brand.service.ts` implementados (antes vazios); `services/product.service.ts` ganhou `getProductsCatalog` (filtros via PostgREST embedding `offers!inner`/`offers!left`, paginação real via `count: "exact"`, ordenação por preço corrigida em memória por página — limitação documentada, ADR-011); `components/product/ProductGrid.tsx` implementado (antes vazio), `ProductGridSkeleton`/`ProductFilters` novos; `hooks/useProductFilters.ts` novo (URL como fonte de verdade); `components/ui/Breadcrumb.tsx`/`Pagination.tsx`/`Input.tsx`/`Select.tsx` novos/preenchidos; `ProductCard`/`ProductHighlightCard` unificados (ADR-010), `ProductBreadcrumb` substituído pelo `Breadcrumb` genérico; `app/products/{page,loading,error}.tsx` novos, com `generateMetadata`, JSON-LD `CollectionPage`+`ItemList`+`BreadcrumbList` e `<Suspense>`.

Proposta de migration (não aplicada): `database/migrations/0003_proposed_product_catalog_price_view.sql` (materialized view para ordenação de preço escalável). Validado com `npm run lint`/`typecheck`/`build` — ver relatório da sprint para o resultado.

## Sprint 3.6 — Data Foundation (auditoria, sem novas telas, sem código alterado)

Sprint de diagnóstico puro, a pedido explícito do CTO ("não implemente funcionalidades de interface", "não execute migrations", "não realize inserts sem aprovação"), preparando a base de dados para Comparação de Produtos, Histórico de Preços, Favoritos, Alertas e ParaguAI AI.

- **Banco**: relacionamentos `products→brands`, `products→categories`, `offers→products`, `offers→stores` reconfirmados sem erro de FK. Nenhuma constraint `UNIQUE`/`NOT NULL` aplicada hoje em `stores.slug` nem em campos obrigatórios de `products`/`offers` — auditoria de índices/constraints não é possível só com a chave anônima (precisa do painel do Supabase ou de uma chave de serviço), registrado como limitação, não como fato verificado.
- **Dados (consulta ao vivo)**: `products: 0`, `offers: 0`, `brands: 0`, `categories: 0`, `stores: 5` (todas com `slug`/`active`/`cover_image` nulos; `website`/`opening_hours` já preenchidos nas 5; `address` preenchido em 4/5; `phone`/`email` nulos em todas; `whatsapp` é string vazia em 1 loja, em vez de nulo — pequena inconsistência de dado, não de schema).
- **Services**: `product.service.ts`, `offer.service.ts`, `store.service.ts`, `search.service.ts`, `category.service.ts`, `brand.service.ts` revisados linha a linha contra o schema real — nenhuma divergência nova encontrada (a correção da Sprint 3.5/ADR-009 segue válida). Nenhuma correção de código necessária antes da Sprint do Comparador.
- **Migrations**: `0001` confirmada superada (mantida só por histórico); `0002` (fase 1, `UNIQUE (slug)`) revisada como segura para aplicar a qualquer momento; `0003` (view de agregação de preço) avaliada como prematura — sem produtos reais, não há nada para agregar.
- **Seed**: proposta de estratégia entregue (não executada) — backfill de `slug` a partir do `name` real de cada loja + dados de exemplo para `brands`/`categories`/`products`/`offers`. Ver relatório completo da sprint para o plano detalhado.

## Sprint 3.7 — Data Foundation v2 (Release 0.5, fundação)

Sistema oficial de seed implementado como código (`database/seed/`), constraints/índices e views de apoio propostos, arquitetura de Price Engine e estratégia de Offer Ranking documentadas — nenhum dado real inserido, nenhuma migration aplicada, nenhuma feature de interface.

- **`database/seed/`** (novo): `brands/`, `categories/`, `stores/`, `products/`, `offers/` (dados de exemplo em JavaScript puro, ver ADR-012), `lib/client.js` (cliente Supabase próprio da ferramenta), `index.js` (orquestrador idempotente, **dry-run por padrão**, só escreve com `--execute`), `validate.js` (auditoria de qualidade de dados, somente leitura). Scripts novos em `package.json`: `db:seed`, `db:seed:execute`, `db:validate`.
- **`eslint.config.mjs`**: `database/seed/**` adicionado a `globalIgnores` (tooling Node fora da árvore TypeScript da aplicação — ADR-012).
- **Migrations propostas (não aplicadas)**: `0004_proposed_catalog_integrity_and_indexes.sql` (`UNIQUE (slug)` em `products`/`brands`/`categories` + índices em `offers.product_id`/`store_id`/`price_usd`, `products.brand_id`/`category_id`); `0005_proposed_store_ranking_view.sql` (`store_ranking_summary`, insumo do Offer Ranking).
- **Arquitetura documentada, não implementada**: Price Engine (ADR-013) e algoritmo de Offer Ranking v1 (ADR-014) — ver `docs/operations/DECISIONS.md`.
- **Validação ao vivo (`db:validate`)**: nenhum problema de qualidade de dado encontrado nas tabelas hoje vazias; achado já conhecido reconfirmado — 5/5 lojas sem `slug`.
- **Services**: revisados novamente (`product`, `offer`, `store`, `search`) — nenhuma divergência nova, nenhuma correção aplicada.

Validado com `npm run lint` (0 erros, 5 warnings pré-existentes), `npx tsc --noEmit` (0 erros) e `npm run build` (sucesso, mesmas 6 rotas).

## Sprint 3.8 — Seed Execution & Catalog Validation

Executa, pela primeira vez, escrita real contra o Supabase de produção: o seed implementado na Sprint 3.7 saiu de dry-run para `--execute`, com aprovação explícita do CTO. Nenhuma migration aplicada, nenhuma feature de interface, nenhuma alteração de arquitetura.

- **Bloqueio real encontrado e resolvido**: a primeira tentativa de `--execute`, só com `NEXT_PUBLIC_SUPABASE_ANON_KEY`, falhou — `INSERT` em `brands`/`categories`/`products` bloqueado por RLS (erro explícito) e o `UPDATE` de `stores.slug`/`active` foi **filtrado silenciosamente** pela RLS (0 linhas afetadas, sem erro, mas o script logou `[OK]` indevidamente — falso positivo confirmado por snapshot antes/depois). Nenhuma escrita real ocorreu nessa tentativa. Resolvido com `SUPABASE_SERVICE_ROLE_KEY` adicionada a `.env.local` pelo CTO. Ver ADR-016.
- **Seed executado com sucesso** (2ª tentativa, com a chave de serviço): `stores` (5/5 backfill de `slug`/`active`), `brands` (5 criadas), `categories` (5 criadas), `products` (6 criados), `offers` (9 criadas). Reexecução confirmou idempotência (`[SKIP]` em tudo, sem duplicata).
- **Auditoria de integridade**: `npm run db:validate` (0 problemas) + auditoria extra com anti-join real (não só `IS NULL`) via chave de serviço — 0 FKs órfãs (`offers.product_id`/`store_id`, `products.brand_id`/`category_id`), 0 slugs duplicados, 0 pares `product_id+store_id` duplicados, 0 produtos inativos. Único "achado" é deliberado: `playstation-5-slim` sem nenhuma oferta (testa o estado vazio da UI, conforme comentário do próprio dado de seed).
- **Nenhuma correção de dados foi necessária** — o seed já carregou de forma consistente.
- **Migrations `0002`/`0004`/`0005`**: continuam propostas, não aplicadas (fora do escopo desta sprint, por instrução explícita).
- **Price Engine (ADR-013) / Offer Ranking (ADR-014)**: continuam apenas documentados, nenhum código novo.

Ver relatório completo da Sprint 3.8 para o detalhe ambiente/snapshot/auditoria.

## Sprint 3.9 — Price Engine v1 + Compare Foundation

Implementa a primeira versão de código (não só arquitetura) do Price Engine proposto na Sprint 3.7 (ADR-013), e corrige o bug de log identificado na Sprint 3.8 (ADR-016). Nenhuma UI nova, nenhuma autenticação, nenhum scraping/IA — conforme escopo explícito da missão.

- **`database/migrations/0006_proposed_price_history.sql`** (novo): tabela `price_history` (`offer_id`, `price_usd`, `price_brl`, `old_price_usd`, `source`, `recorded_at`) + índice composto `(offer_id, recorded_at DESC)`. **Proposta, não aplicada** — bloqueio de ferramenta, não de aprovação: nenhum mecanismo disponível neste projeto executa DDL contra o Supabase (sem `pg`/`DATABASE_URL`, sem Supabase CLI, sem RPC de SQL exposta). Ver ADR-017.
- **`types/priceHistory.ts`** (novo): `PriceHistoryEntry`, `PriceChangeSource`, `OfferPriceMetrics`, `PriceUpdateResult`.
- **`services/offer.service.ts`**: `updateOfferPrice()` (caminho único de escrita de preço, grava histórico antes de atualizar a oferta, no-op se o preço não mudou, confirma linhas afetadas) e `getOfferPriceMetrics()` (menor/maior preço histórico, variação percentual, última mudança — insumo do futuro `/compare`). Ambos testados ao vivo contra o Supabase real: degradação graciosa confirmada (preço atual real, campos de histórico `null`) enquanto `price_history` não existir.
- **`database/seed/index.js`**: corrigido o bug do ADR-016 — o backfill de `stores` agora confirma linhas afetadas (`.select("id")`) antes de logar `[OK]`, e loga `[AVISO]` quando a RLS filtra a escrita silenciosamente. Testado contra o Supabase real com a chave anônima (reproduz o bug original) — detecção confirmada.
- **`docs/operations/DECISIONS.md`**: ADR-017 (schema do Price Engine, caminho único de escrita, bloqueio de DDL).

Validado com `npm run lint` (0 erros, 5 warnings pré-existentes), `npx tsc --noEmit` (0 erros), `npm run build` (sucesso, mesmas 6 rotas), `npm run db:validate` (0 problemas) e reexecução de `npm run db:seed:execute` (idempotência confirmada, tudo `[SKIP]`).

**Não incluído, por instrução explícita**: nenhuma UI/página nova (`/compare` segue sem interface); nenhuma autenticação; nenhuma migration aplicada (`0002`/`0004`/`0005`/`0006` continuam propostas); nenhuma alteração de RLS.

### Adendo (mesmo dia) — Price Engine validado contra dados reais + achado crítico de RLS

O CTO aplicou `0006_proposed_price_history.sql` manualmente no SQL Editor do Supabase. Isso permitiu validar o Price Engine de ponta a ponta contra a tabela real, em vez de só a degradação graciosa.

- **Bug real encontrado e corrigido** (antes de qualquer escrita real): `getOfferPriceMetrics` calculava `highestPriceUSD`/`priceChangePercent` sem considerar o preço original (capturado só em `old_price_usd` da primeira entrada de histórico) — se o preço só tivesse caído, o pico original nunca apareceria. Corrigido em `services/offer.service.ts`. Ver ADR-018.
- **27 asserções passaram** num teste funcional completo contra a oferta real `iphone-16-pro-256gb-titanio-preto@cellshop`: leitura de histórico vazio, métricas baseline, duas mudanças reais de preço (999→949→1050), no-op corretamente detectado, restauração ao preço original (999, preservando 3 entradas reais de histórico), métricas finais corretas (`lowest=949`, `highest=1050`).
- **Classificação**: Price Engine v1 é **"Backend Production Ready"** — não "Production Ready" de ponta a ponta.
- **Achado crítico, não corrigido (ADR-019)**: testando a leitura com a chave anônima (a que a aplicação usa), confirmou-se que ela não vê nenhuma linha de `price_history` — nem de `brands`/`categories`/`products`/`offers`. Só `stores` tem leitura pública funcionando. Isso significa que o catálogo real provavelmente está vazio para usuários reais desde a Sprint 3.8, e nenhuma auditoria anterior pegou isso porque passou a usar, sem essa intenção, a chave de serviço (presente em `.env.local` desde a Sprint 3.8) em vez da chave anônima. Correção proposta, não aplicada: `database/migrations/0007_proposed_public_read_policies.sql`.

Validado de novo: `npm run lint`/`tsc --noEmit`/`npm run build` (sem regressão) e `npm run db:validate` (0 problemas).

## Sprint 4.0 — Compare Engine v1 (Release 0.5)

Entrega o Compare Engine v1: compara um produto entre todas as lojas disponíveis usando dados reais, com Price Engine integrado e algoritmo de ranking de ofertas (ADR-014, primeira implementação). Primeiro MVP visível da plataforma.

- **`types/compare.ts`** (novo): `RankedOffer`, `CompareSummary`, `CompareResult`.
- **`services/compare.service.ts`** (novo): `getProductComparisonBySlug`/`getProductComparison` — motor de comparação com 3 queries (produto + ofertas com loja + batch de price_history via `.in()`), ranking em memória (ADR-014), cálculo de resumo. Ver ADR-020.
- **`app/api/compare/route.ts`** (novo): GET `/api/compare?slug=` ou `?productId=` — endpoint JSON com headers de cache.
- **`hooks/useCompare.ts`** (novo): `useCompare(slug)` — hook client-side.
- **`components/compare/CompareSummary.tsx`** (novo): resumo min/max/savings.
- **`components/compare/CompareOfferCard.tsx`** (novo): card de oferta rankeada com histórico de preços e score.
- **`app/compare/[slug]/{page,loading,not-found,error}.tsx`** (novos): rota SSR Server Component, `generateMetadata`, todos os estados de UI.
- **`constants/routes.ts`**: `comparePath()`/`compareUrl()` adicionados.
- **`database/seed/validate_compare.js`** (novo): 6 cenários validados contra o Supabase real com a chave de serviço — todos passaram.

**Validação**: lint (0 erros), tsc (0 erros), build (8 rotas — `+/api/compare`, `+/compare/[slug]`), db:validate (0 problemas). Compare Engine validado fim a fim com a chave de serviço (6 cenários, todos OK). Chave anônima confirmada bloqueada (ADR-019, pré-existe à sprint).

---

## Sprint 4.2 — MVP Público (Release 0.7) — 2026-06-25

Lapidação completa da experiência existente antes do primeiro lançamento público. Nenhuma funcionalidade nova — foco em qualidade, SEO, navegação e performance.

- **`next/image` completo**: todos os 5 `<img>` substituídos (ProductCard, ProductGallery, StoreCard, store/[slug]/page) — 0 warnings de lint. `next.config.ts` com `remotePatterns` para Supabase Storage + HTTPS genérico.
- **Navegação sem links mortos**: Navbar simplificada (Início / Produtos / Buscar / IA); Footer converte links inexistentes em badges "em breve" — nenhum link aponta para 404.
- **SEO**: `app/sitemap.ts` (dinâmico, busca produtos + lojas do Supabase em paralelo → `/sitemap.xml`), `app/robots.ts` → `/robots.txt`. JSON-LD `Organization` no root layout. Metadata completa (OG + Twitter) na Home.
- **UX**: `app/not-found.tsx` (404 global com design de marca).
- **Performance**: double-fetch eliminado em `/compare/[slug]` via `cache(getProductComparisonBySlug)`.
- **Validações**: lint 0 erros/warnings, tsc 0 erros, build 10 rotas, db:validate 0 problemas.

## Sprint 4.3 — Data Integrity & Media Foundation (Release 0.7 — fase final) — 2026-06-25

Última etapa técnica antes da declaração do Release 0.7.

- **Migration `0008_data_integrity.sql`** (criada, aguarda SQL Editor): consolida 0002 + 0004 num único SQL idempotente. UNIQUE constraints em `slug` para todas as 4 tabelas de catálogo + 6 índices de performance (FKs + price_usd + price_history). Auditoria pré-migração: 0 duplicatas, 0 orphans, 0 nulos — banco já íntegro, migration só formaliza as garantias.
- **Storage Foundation**: bucket `catalog` criado no Supabase Storage (público, webp/jpeg/png/avif, 5 MB/arquivo). `utils/storage.ts` novo — builders de URL tipados + `resolveImageUrl` com fallback automático para Storage. `database/storage/init.js` (`npm run storage:init`). ADR-022.
- **Validação Sprint 4.3**: `database/seed/validate_sprint43.js` (`npm run db:validate:43`) — 23 asserções: contagem, slugs, FKs, preços, Storage. 23 OK | 0 falhas.
- **ESLint**: `database/storage/**` adicionado ao `globalIgnores` (tooling Node).
- **Documentação**: ADR-022 + ADR-023 em DECISIONS.md; Sprint 4.3 no CHANGELOG.md.
- **Validações**: lint 0, tsc 0, build 10 rotas (sem regressão), db:validate 0, db:validate:43 23/23 OK.

## Release 0.8 — Go Live Foundation — 2026-06-25

- **Imagens**: 16 URLs de placehold.co inseridas no banco (products, stores, brands) via `database/seed/update_images.js`. Catálogo agora exibe imagens representativas.
- **Favicon + PWA**: `app/icon.tsx` (512×512 PNG) + `app/apple-icon.tsx` (180×180) via `ImageResponse` + `app/manifest.ts` (manifest.webmanifest). 3 novas rotas estáticas.
- **Analytics**: `components/analytics/Analytics.tsx` (GA4 + Clarity, graceful degradation) + `utils/analytics.ts` (event helpers tipados). `CompareOfferCard` + `StoreCard` rastreiam cliques. Layout inclui `<Analytics />`.
- **SEO**: `viewport` com `themeColor` + `colorScheme`. Suporte a Google Search Console e Bing Webmaster via env vars. `<link rel="preconnect">` para Supabase. Twitter card com `site`.
- **Segurança**: 6 security headers em `next.config.ts` (HSTS, X-Frame-Options, nosniff, Referrer-Policy, Permissions-Policy).
- **Scripts**: `db:images`, `db:images:dry-run`.
- **Validações**: lint 0, tsc 0, build 13 rotas, db:validate 11 OK, db:validate:43 23/23.

## Status Geral: **95%**

Release 0.8 entregue: site com imagens, favicon, manifesto, analytics, headers de segurança, SEO avançado.

---

## Release 1.1 — First Live Connector — 2026-06-26

Primeira loja real integrada: Shopping China (`shoppingchina.com.py`). FetchEngine com `HttpFetchStrategy`, parser de listagem e detalhe HTML, conector com rate limiting, persistência via `CatalogWriter`. 30 produtos reais em 3 categorias. Dry-run validado → execute → idempotência confirmada. Migrations aplicadas: 0010 (store + connector_configs) + 0011 (UNIQUE constraint em offers).

## Release 1.2 — Merchant Operating System — 2026-06-26

Portal self-service completo para lojistas (SaaS). 6 novas tabelas (merchant_plans, merchants, merchant_stores, merchant_audit_logs, merchant_analytics_events, merchant_recommendations). 10 API routes. 11 páginas `/merchant/*`. Onboarding wizard 5 passos. Dashboard com Merchant Score (0-100) e recomendações automáticas. Auditoria completa de ações. Plans engine com 4 planos seed.

**ADRs**: ADR-031 a ADR-035 (ver DECISIONS.md)

## Release 1.3 + Hotfixes Auth/Dashboard — 2026-06-27

**Release 1.3 — Dashboard Consultivo**: redesign completo. `NextStepCard`, `GoalsPanel`, 6 níveis de lojista (`MerchantLevel`), `computeNextStep`, `computeGoals`. API `/stats` estendida com `level`/`nextStep`/`goals`. `ScoreCard`/`RecommendationsPanel`/`StatsGrid` redesenhados.

**Hotfix Auth (PKCE)**: `app/auth/callback/route.ts` criado — troca code PKCE por sessão e coloca cookies. `emailRedirectTo` usa `window.location.origin` (resolve mismatch de porta). Quando verifier PKCE está ausente (link aberto em browser diferente), redireciona para `?confirmed=true` em vez de erro. Login page mostra banner verde.

**Hotfix Dashboard**: `requireMerchant()` agora checa `merchants` diretamente via service role key — não depende mais de `profiles.role`. Elimina o loop de 403 causado pelo email confirmation flow. Error states contextuais no dashboard (not_found/server/network). Migration `0013` corrige o constraint `profiles_role_check`.

**Fluxo completo validado**: Cadastro → e-mail → confirmação → login → dashboard carregando ✓

**Pendência**: Migration `0013_fix_profiles_role_merchant.sql` — aplicar no Supabase SQL Editor (idempotente, segura).

## Release 1.3.1 — ParaguAI Experience Integration — 2026-06-27

Release exclusivamente de UX, navegação e branding. Zero alterações no Merchant OS, auth, APIs ou banco.

- `HeroCTAs` — CTAs auth-aware no Hero: "Comparar preços" + "Sou Lojista" / "Minha Loja"
- `ForLojistasSection` — Seção premium para lojistas na Home (6 benefícios + CTA duplo)
- Navbar — "Para Lojistas" no menu; botão "Entrar" → `/merchant/login`
- Footer — Coluna "Para Lojistas" (Cadastrar Loja / Planos / Central do Lojista / Ajuda)
- `/para-lojistas` — Landing page institucional (Hero + Benefícios + Como Funciona + Planos + Importação + FAQ + CTA)
- Branding: user-facing usa "Área do Lojista" / "Central do Lojista" — nunca "Merchant Portal"
- SEO: metadata + canonical + OG + Twitter em `/para-lojistas`
- Qualidade: lint 0, tsc 0, build OK (64 rotas + `/para-lojistas`), zero regressões
